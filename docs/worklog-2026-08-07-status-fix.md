# Work Log — 2026-08-07 (Session 2)

## Task Name
Fix Case-Relative Status Lookup and Route Button Cooldown Display on the Case Map Staff Panel

## Spending Hour
~1.5 ชั่วโมง (ประมาณการจากขอบเขตงาน — ไม่มีการจับเวลาจริงระหว่างทำงาน ควรปรับตามเวลาที่ใช้จริง)

## Activity (สิ่งที่ทำ)
- ตรวจสอบต้นตอของปัญหาที่การ์ดเจ้าหน้าที่แสดงสถานะของเคสเป็นเครื่องหมาย "-" แทนที่จะเป็นชื่อสถานะจริง โดยไล่ดูโค้ดย้อนกลับไปถึงแหล่งข้อมูล พบว่าโค้ดเดิมนำรหัสสถานะเฉพาะเคส (ซึ่งเป็นคนละชุดกับสถานะปฏิบัติหน้าที่ทั่วไปของเจ้าหน้าที่) ไปเทียบกับรายการสถานะที่ผิดชุด ทำให้หาไม่เจอและแสดง "-" เสมอ
- เพิ่มฟังก์ชันใหม่สำหรับดึงและเทียบรายการสถานะของเคสให้ถูกชุด แล้วนำไปใช้แทนที่จุดแสดงผลเดิมทั้งในการ์ดเจ้าหน้าที่รายบุคคลและรายการกลุ่มเจ้าหน้าที่ โดยไม่กระทบการแสดงสถานะปฏิบัติหน้าที่ทั่วไปที่ทำงานถูกต้องอยู่แล้ว
- แก้ไขปุ่ม "คำนวณเส้นทาง" ให้ไม่แสดงตัวเลขนับถอยหลัง (เช่น "รอ 7 วินาที") ขณะที่ปุ่มถูกปิดใช้งานชั่วคราวหลังกดไปแล้ว โดยยังคงระยะเวลาหน่วงตามเดิมไว้ (ปุ่มยังกดซ้ำทันทีไม่ได้) เพียงแค่ไม่แสดงตัวเลขบนหน้าจอ และย้ายข้อมูลเวลารอไปไว้ในรูปแบบที่โปรแกรมอ่านหน้าจอ (screen reader) ยังใช้งานได้อยู่
- ตรวจสอบผลด้วย `tsc -b`, `eslint` และ `vite build` หลังแก้ไขทุกครั้ง เพื่อยืนยันว่าไม่มีปัญหาการคอมไพล์หรือ type ผิดพลาด
- อัปเดต `src/changelog.json` และ `package.json` (bump เวอร์ชัน 0.43.0 → 0.43.1) ด้วยข้อความสรุปภาษาที่ไม่ใช่เชิงเทคนิค

## Scope / Context (ขอบเขต / บริบทงาน)
- แก้ไขปัญหาที่พบจากการใช้งานฟีเจอร์แสดงสถานะเฉพาะเคสและฟีเจอร์คำนวณเส้นทาง ซึ่งเพิ่งพัฒนาไปในเซสชันก่อนหน้าของวันเดียวกัน (อ้างอิง `docs/worklog-2026-08-07.md`)
- ครอบคลุมเฉพาะส่วนแผนที่เคสในการมอบหมายเจ้าหน้าที่ (staff dispatch panel) ไม่ได้แตะการทำงานอื่นของระบบ

## Outcome (ผลลัพธ์ / สิ่งที่เปลี่ยนแปลง)
- สถานะของเจ้าหน้าที่เฉพาะเคสที่กำลังดูอยู่ (เช่น ถูกสั่งการ / ถึงที่เกิดเหตุ) แสดงชื่อสถานะจริงแล้ว แทนที่จะเป็น "-" ทั้งในการ์ดรายบุคคลและรายการกลุ่ม
- ปุ่มคำนวณเส้นทางไม่แสดงตัวเลขนับถอยหลังอีกต่อไประหว่างที่ถูกปิดใช้งานชั่วคราว แต่ยังคงพฤติกรรมการหน่วงเวลาเดิมไว้ครบถ้วน
- ผ่านการตรวจสอบ type-check, lint และ production build เรียบร้อย (ยังไม่ได้ทดสอบผ่านหน้าจอจริงในเบราว์เซอร์ ควรตรวจสอบเพิ่มเติมก่อนขึ้นระบบจริง)
- อัปเดต changelog เป็นเวอร์ชัน 0.43.1

## Files Changed
- `src/cms/components/case/createCase/map/staff/staffDisplay.ts`
- `src/cms/components/case/createCase/map/staff/StaffDetailPanel.tsx`
- `src/cms/components/case/createCase/map/staff/StaffGroupPanel.tsx`
- `src/cms/components/case/createCase/map/staff/StaffRouteCalculateButton.tsx`
- `src/changelog.json`
- `package.json`
