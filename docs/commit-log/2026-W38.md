# Commit Log — Week 38, 2026

## Monday — 2026-09-14

### Case Summary by Area Dashboard

Commit: `820cc14`

`feat(cms): add a Case Summary by Area dashboard`

Adds `CaseSummaryByAreaPage`/`CaseSummaryByAreaDashboard`/`CaseSummaryByAreaTable`
under `src/cms`, a `caseSummaryByArea.json` mock fixture, and a new
`case-summary-by-area` widget source (type + parser + registry entry) in
`src/core/components/custom-dashboard/sources/`. Registers the route in
`src/cms/App.tsx` behind the existing `dashboard.view` permission, and adds
the matching nav entries to both `AppSidebar.tsx` and `SuperTopbar.tsx` — the
audit for the topbar mirror also caught a pre-existing gap for the
`area-template` entry, fixed in the same commit. i18n keys added to
`en`/`th`/`cn` catalogs. A new anti-regression lesson documents the
sidebar/topbar parity requirement going forward. `tsc -b` passes.

Commit: `0c82e9d`

`chore(release): publish the Case Summary by Area dashboard as 0.75.0`

Version bump only: `package.json` 0.74.0 → 0.75.0, matching `src/changelog.json` entry.

### CLAUDE.md Documentation Accuracy Refresh

Commit: `01ff71d`

`docs: refresh CLAUDE.md architecture and build notes`

Single-file documentation correction, independent of the dashboard feature
above — updates the map-provider, `kms` module, build-memory, and
manualChunks sections to match current code.

---

**Note on scope:** No commits existed for 2026-09-14 before this run (the
last prior commit was 2026-09-11). All three commits above package
pre-existing uncommitted working-tree changes, attributed to today based on
file mtimes. The Case Summary by Area feature and the CLAUDE.md refresh are
unrelated in purpose and kept as separate commits per the grouping rule, even
though both surfaced during the same working day.

**Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
`docs/task-evidence/`, `docs/tickets/`, `docs/business-requirement/`,
`docs/map-provider-decision-memo*.docx`,
`docs/MASTER PROMPT — Personal Engineering Evidence Manager.md`,
`public/longdo-spike.html` and `src/core/mocks/permissionCURL.sh` — personal
engineering-evidence material, business-requirement drafts, and a scratch
reference/probe file, left for the developer to include or not. No push was
performed; commits are left for manual review.

## Tuesday — 2026-09-15

### Compact Dashboard Widget Tiles and Top Districts Widget

Commit: `1cad6e1`

`feat(core): add compact widget tiles and a Top Districts widget`

Adds a "compact" display mode to the custom-dashboard widget framework
(`src/core/components/custom-dashboard/widgets/types.ts`, `registry.ts`,
`CompactWidgetTile.tsx`, `compactAdapters.ts` + test) so any widget can be
shown as a single icon+number tile. Splits `WidgetDefinition` into `sourced`
(existing WebSocket-fed widgets) and `self-fetching` kinds in `types.ts` and
`WidgetHost.tsx`, the base the Dispatch and Case-Area-map widgets build on
later today. Adds a new `CaseSummaryByAreaTopDistrictsWidget` to the library.
i18n keys added to `en`/`th`/`cn`. `tsc -b` passes.

Commit: `5d76d98`

`chore(release): publish compact widget tiles and Top Districts widget as 0.76.0`

Version bump only: `package.json` 0.75.0 → 0.76.0, matching `src/changelog.json` entry.

### Case Summary by Area Map View and Table UX Improvements

Commit: `3089869`

`feat(cms): add a map view to the Case Summary by Area dashboard`

Adds a Map tab (`CaseAreaChoroplethMap` + per-provider ArcGIS/MapLibre-MapTiler/
Longdo implementations, legend, unavailable-boundary notice) under
`src/cms/components/dashboard/map/`, wired into `CaseSummaryByAreaDashboard.tsx`
via a `Tabs` component alongside the existing table. Adds a self-fetching
`CaseSummaryByAreaHeatmapWidget` registered from
`registerCaseSummaryByAreaHeatmapWidget.ts`. Makes the table's header row and
total-row strip sticky (`CaseSummaryByAreaTable.tsx`, plus a `style` prop
added to the shared `TableCell` in `src/core/components/ui/table/index.tsx`).
i18n keys added to `en`/`th`/`cn`. `tsc -b` passes.

Commit: `8713bcc`

`chore(release): publish the Case Summary by Area map view as 0.77.0`

Version bump only: `package.json` 0.76.0 → 0.77.0, matching `src/changelog.json` entry.

### Dispatch Dashboard

Commit: `36c688f`

`feat(cms): add a Dispatch Dashboard`

New `DispatchDashboard.tsx` / `DispatchDashboardPage.tsx` plus a `dispatch/`
folder (KPI row, officer status panel/widget, events list/widget, heatmap
widget and their data hooks) and a `map/heatmap/` folder (per-provider
incident heatmap implementations), registered via
`registerDispatchWidgets.ts`. Adds the `/cms/dashboard/dispatch` route to
`src/cms/App.tsx` (also wires in the heatmap registration import from the
previous commit's follow-up work), and matching nav entries to
`AppSidebar.tsx` and `SuperTopbar.tsx`. i18n keys added to `en`/`th`/`cn`.
`tsc -b` passes; `pnpm test` (vitest) passes all 75 tests across 10 files.

Commit: `6ed931f`

`chore(release): publish the Dispatch Dashboard as 0.78.0`

Version bump only: `package.json` 0.77.0 → 0.78.0, matching `src/changelog.json` entry.

---

**Note on scope:** No commits existed for 2026-09-15 before this run. All
three feature commits package pre-existing uncommitted working-tree changes,
attributed to today based on file mtimes (12:23–17:17). The three features
are related (the second and third build on the widget-framework change from
the first) but serve distinct, separately-shippable purposes, so each got its
own feature commit and release commit, matching this project's established
one-feature-per-release convention. `public/i18n/{en,th,cn}.json` is shared
across all three features; each commit's diff to it was produced by
reconstructing the file from the pre-existing HEAD version plus only the
i18n keys that belong to that commit (verified byte-for-byte equal to the
final working-tree file once all three commits are applied), rather than
splitting the working tree's single combined edit by hand.

Three customer-profile files (`CustomerCreate.tsx`, `CustomerView.tsx`,
`SocialAccountManager.tsx`) and `src/core/mocks/permissionCURL.sh` were
modified 2026-09-14 (yesterday, right after the last prior commit) and are
intentionally excluded from today's commits.

**Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
`docs/task-evidence/`, `docs/tickets/`, `docs/business-requirement/`,
`docs/map-provider-decision-memo*.docx`,
`docs/MASTER PROMPT — Personal Engineering Evidence Manager.md` and
`public/longdo-spike.html` — personal engineering-evidence material and
business-requirement drafts, left for the developer to include or not. No
push was performed; commits are left for manual review.
