# Commit Log — Week 39, 2026

## Monday — 2026-09-21

### Staff Workload Ranking and Map Filter

Commit: `46b5dcf`

`feat(cms): rank staff by workload in the map staff filter`

### Case Map Case Panel and Drag-and-Drop Assignment

Commit: `ef86a97`

`feat(cms): add case panel, drag-to-assign and focus tools to the case map`

Case Panel, connector lines, focus, and drag-to-assign share the map field and
detail-view wiring, so they are committed together. Includes Friday's uncommitted
map symbol/theme polish because the map files depend on it.

### Release

Commit: `27997af`

`chore(release): publish the richer case map as 0.81.0`

---

**Note on scope:** No commits existed for 2026-09-21 before this run. Customer-profile files (mtimes 2026-09-14) remain uncommitted and excluded.

## Tuesday — 2026-09-22

### Place/Device Admin List Filters

Commit: `6414579`

`feat(cms): add category and device-type filters to Place/Device management`

### Officer Drag-to-Assign Multi-Provider Integration and Case Panel Consolidation

Commit: `348b69c`

`feat(cms): wire officer drag-to-assign into all map providers, merge case panel people list`

Includes Monday's uncommitted drag/drop, screen-projector and anchored-overlay
groundwork (ArcGIS-only, untested) because today's integration files import
them directly - splitting the commit would leave a broken build at that point
in history.

### Thailand Nationwide Boundary Data Regeneration

Commit: `cb38ad1`

`feat(cms): regenerate nationwide Thailand admin boundaries as split TopoJSON`

### Release

Commit: `db25f3f`

`chore(release): publish drag-to-assign parity and boundary map coverage as 0.82.0`

---

**Note on scope:** The generator script's raw source exports (`src/cms/mocks/TH_Country_Province.json`, 21MB, and `TH_Country_District.json`, 57MB) were left uncommitted - only the generated `public/geo/thailand/` output was committed. The existing Bangkok-only equivalents in `src/cms/mocks/` are committed and tiny (under 1MB combined); these nationwide raw exports are ~100x larger, so committing them was treated as a judgment call for the developer rather than assumed. Also excluded as unrelated/stale: `CustomerCreate.tsx`, `CustomerView.tsx`, `SocialAccountManager.tsx` (mtimes 2026-09-14), various `*CURL.sh` / `areaGO.txt` mocks, `public/longdo-spike.html`, and the `Claude outputs/` / `docs/business-requirement/` / `docs/screen-capture/` / `docs/specification/` / `docs/tickets/` note directories (all pre-existing, untouched today).

## Wednesday — 2026-09-23

### Place and Device Marker Clustering on the Case Map

Commit: `b279b70`

`feat(cms): add proximity clustering to Place and Device map markers`

### Administrative Boundary Matching Fix and Build Tooling Hardening

Commit: `a17ed5e`

`fix(cms): fix boundary geometry matching for districts and country level`

### Unit Management List Loading Fix

Commit: `559ec97`

`fix(cms): load full unit and property lists in Unit Management`

### Release

Commit: `84e3696`

`chore(release): publish marker clustering and boundary fixes as 0.83.0`

---

**Note on scope:** The raw boundary source exports moved today to `boundary-source-data/` at the repo root (`TH_Country_Province.json` / `TH_Country_District.json`, ~79MB combined) remain uncommitted, for the same reason as their previous location under `src/cms/mocks/` yesterday - the generated `public/geo/thailand/` output is what ships. `tsc -b` and `vitest` (22 files / 231 tests) both passed clean before committing. Excluded as unrelated/stale: `CustomerCreate.tsx`, `CustomerView.tsx`, `SocialAccountManager.tsx` (mtimes 2026-09-14, unchanged today), various `*CURL.sh` / `areaGO.txt` mocks, `public/longdo-spike.html`, `docs/map-provider-decision-memo*.docx`, and the `Claude outputs/` / `docs/business-requirement/` / `docs/screen-capture/` / `docs/specification/` / `docs/task-evidence/` / `docs/tickets/` / `docs/MASTER PROMPT...md` note items (all pre-existing, untouched today per file mtimes).

## Thursday — 2026-09-24

### Case Map Legend and Bottom-Left Layout Rework

Commit: `dc122a1`

`feat(cms): add case map legend and align bottom-left overlay layout`

### Longdo Map Initialization Crash Fix

Commit: `5a6dc72`

`fix(cms): defer Longdo map basemap/init until SDK ready event`

Split out of `LongdoAddressMap.tsx`'s combined working-tree diff via `git apply
--cached` on the layout-only hunks first, so this commit holds only the
async-init fix and the previous commit holds only the layout change - the two
were unrelated changes that happened to land in the same file today.

### District Boundary Refresh Fix (Longdo / MapTiler)

Commit: `f255b93`

`fix(cms): refresh district boundaries when province selection changes`

### Release

Commit: `07438a1`

`chore(release): publish case map legend and reliability fixes as 0.84.0`

---

**Note on scope:** `Legend.tsx` is a new component and is not yet imported/rendered
anywhere (`AddressMapField.tsx` only references it in a comment) - committed as-is
since it is genuinely today's work, just not wired up yet. Excluded as unrelated/stale
(all older than today by file mtime, none touched today): `MapPlaceButton.tsx`,
`device/MapDeviceButton.tsx`, `device/deviceSymbols.ts`, `place/placeSymbols.ts`,
`staff/staffSymbols.ts` (mtimes 2026-09-23), `CustomerCreate.tsx`, `CustomerView.tsx`,
`SocialAccountManager.tsx` (mtimes 2026-09-14), various `*CURL.sh` / `areaGO.txt` mocks,
`public/longdo-spike.html`, `docs/map-provider-decision-memo*.docx`, and the
`Claude outputs/` / `boundary-source-data/` / `docs/business-requirement/` /
`docs/screen-capture/` / `docs/specification/` / `docs/task-evidence/` / `docs/tickets/`
/ `docs/MASTER PROMPT...md` note items.
