# Commit Log — Week 37, 2026

## Monday — 2026-09-07

### Map Place-Search Results Stacking Fix

Commit: `93abd48`

`fix(cms): lift the map place-search results above the on-map staff card`

Three files, one logical change: the place-search results list on the case map
shares the top-left column with the staff detail / group card
(`CaseStaffMapField`), and the card — rendered last — was covering the lower
results. `LongdoAddressMap.tsx` and `MapTilerAddressMap.tsx` raise the search
container from `z-10` to `z-20` (one step above the map's own overlays), and
`src/globals.css` gives the ArcGIS Search widget's `.esri-ui-top-left` corner a
matching `z-index: 20` because the SDK leaves that corner without one. Only the
Search widget sits in that corner (Zoom / Compass are moved to `bottom-right`),
so nothing else rides along. `tsc -b` passes on the tree.

### Changelog Entry for the Map Search Stacking Fix

Commit: `69aa079`

`chore(release): publish the map search stacking fix as 0.68.1`

`src/changelog.json` + `package.json` bumped 0.68.0 → 0.68.1. PATCH: a
display-order fix to an existing feature, no new capability. User-visible today —
the results list was unreachable behind the staff card on any map style.

### Weekly Documentation Records

`docs: update weekly development records`

`docs/work-log/2026-W37.md` and `docs/commit-log/2026-W37.md` only (new files for
week 37).

---

**Note on timing:** the source edits in commit `93abd48` were saved on the
evening of 2026-09-04, after that day's documentation run, and were left
uncommitted over the weekend. They were reviewed, committed and documented in
this run. No other development activity occurred on 2026-09-07.

**Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
`docs/task-evidence/`, `docs/map-provider-decision-memo*.docx`,
`docs/MASTER PROMPT — Personal Engineering Evidence Manager.md` and
`public/longdo-spike.html` — personal engineering-evidence material and a scratch
SDK probe page, left for the developer to include or not, as in prior weeks. No
push was performed; commits are left for manual review.
