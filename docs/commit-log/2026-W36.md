# Commit Log — Week 36, 2026

## Monday — 2026-08-31

### Knowledge Base Data Freshness and Dashboard Corrections

Commit: `624696c`

`fix bug kb / delete bk file / fix bug dashboard / fix declared but its value is never read. KbTrendBlock/index.tsx / merge code kb`

Committed manually earlier in the day, before this round. The message does not
follow the conventional-commit format used elsewhere in this log; it is recorded
here as it stands rather than rewritten.

### Changelog Entry for the Knowledge Base Fixes

Commit: `697f93f`

`chore(release): publish the knowledge base fixes as 0.66.6`

`624696c` landed without a changelog entry or a version bump, so both were added
separately here rather than by amending a commit that is already in the branch.

### Weekly Documentation Records

`docs: update weekly development records`

---

**Verification note (2026-08-31):** the map provider work (Longdo as a second map
provider behind a provider-neutral map layer) is **not** committed. It was still
being edited while this round ran — the staff-overlay files were written at
17:38–17:40 — and `tsc -b` fails on the tree in its current state
(`longdo/staff/longdoStaffMarkers.ts`: `pointer` is not declared on
`LongdoMarkerOptions` in the hand-written Longdo typings, 3 errors). Committing a
snapshot of a change that is mid-edit and does not build would put a broken
revision in the branch, so it is left in the working tree for the developer to
finish and commit. The tree did type-check cleanly at 17:36, before those files
appeared, so the failure is the in-flight step, not the earlier work.

The change is recorded in full in the work log and in
`docs/task-evidence/TASK-2026-CMS-0001-map-provider-abstraction.md` (untracked).

`src/changelog.json` carries no entry for the map work: `MAP_PROVIDER` still
defaults to ArcGIS, no environment is switched over, and nothing about it is
visible to a user yet.

Two documentation artifacts from today are deliberately left untracked:
`docs/MASTER PROMPT — Personal Engineering Evidence Manager.md` and
`docs/task-evidence/`. They are personal engineering-evidence material rather
than project documentation, and committing them to a shared repository is the
developer's call, not this workflow's. `public/longdo-spike.html` is likewise
left untracked — it is a scratch page for probing the Longdo SDK and would ship
in the build output if committed.

No push was performed; commits are left for manual review.

---

## Tuesday — 2026-09-01

### Map Provider Abstraction — Longdo Sketch Overlay and ArcGIS Cleanup

Commit: `1d32ce9`

`feat(cms): add a provider-neutral map layer with a Longdo implementation`

This commit carries the whole map-provider change, both Monday's batch (left in
the working tree because `tsc -b` failed on it) and Tuesday's completion — the
Longdo boundary-sketch overlay, the ArcGIS component retype/extraction, the
`ringsSignature` bundle-leak fix, the `VITE_MAP_PROVIDER` / `VITE_LONGDO_API_KEY`
config, and the place-search translations. `tsc -b` passes cleanly on the tree
before committing. `.agents/skills/anti-regression/SKILL.md` rides along: its new
entry is the lesson from the bundle leak fixed in this commit.

ArcGIS stays the default; `src/changelog.json` carries no entry for this work
because no environment is switched to Longdo and nothing is user-visible yet.

### Service Type Price and Screen Improvements

Commit: `fb54b56`

`feat(cms): manage service type price, status and attachment`

The `price` label strings in `public/i18n/*.json` were staged as their own hunk
(`git add -p`) so the map-search strings in the same files stayed with the map
commit.

### Remove Service Type from the Appointment Form

Commit: `938e808`

`refactor(cms): drop the service type field from the appointment form`

Split from the service-type commit above: it removes a dependency on service
types from a different screen rather than extending service-type management.

### Changelog Entry for the Service Type and Appointment Changes

Commit: `a770d48`

`chore(release): publish the service type and appointment changes as 0.67.0`

`src/changelog.json` + `package.json` bumped 0.66.6 -> 0.67.0. MINOR: the price
and status fields are a new capability, not only a fix.

---

**Untracked, deliberately left so (2026-09-01):** `docs/MASTER PROMPT — Personal
Engineering Evidence Manager.md`, `docs/task-evidence/`, and
`public/longdo-spike.html` — same reasoning as the Monday note above. No push was
performed; commits are left for manual review.

---

## Wednesday — 2026-09-02

### MapTiler Map Provider (MapLibre GL)

`feat(cms): add a MapTiler map provider on MapLibre GL`

One commit for the whole third provider: the `maptiler/` implementation module
(map component, SDK bootstrap and tile-worker pin, search box, basemap
catalogue, geometry/symbol helpers, and the boundary, sketch and staff
overlays), the MapTiler geocoding service and the OpenRouteService routing
service, the `VITE_MAP_PROVIDER` / `VITE_MAPTILER_API_KEY` / `VITE_ORS_API_KEY`
config and `MapProviderId` widening, the lazy-load wiring in `AddressMap.tsx`
and `routeService.ts`, the `PlaceCandidate` lift into `geocodeService.ts` and
the `ROUTE_TOKENS` extraction in `routeSymbols.ts`, the `maplibre-gl` dependency
(`package.json` / `pnpm-lock.yaml`), and the `vite.config.ts` `optimizeDeps`
exclude and `manualChunks` guard. `tsc -b` passes cleanly on the tree before
committing.

ArcGIS stays the default. `src/changelog.json` carries **no** entry and there is
**no** version bump — identical reasoning to the Longdo provider commit
(`1d32ce9`): no environment selects MapTiler, so nothing is user-visible yet.

### Weekly Documentation Records

`docs: update weekly development records`

`docs/work-log/2026-W36.md` and `docs/commit-log/2026-W36.md` only.

---

**Untracked, deliberately left so (2026-09-02):** `docs/map-provider-decision-memo.docx`
and `docs/map-provider-decision-memo-th.docx` (the stakeholder decision memo, EN +
TH) and `docs/screen-capture/maptiler-20260902-1330.png` (a MapTiler render
verification shot) — binary artifacts whose inclusion in the shared repo is the
developer's call, the same stance the Monday and Tuesday notes took for the
evidence material. `docs/MASTER PROMPT — Personal Engineering Evidence
Manager.md`, `docs/task-evidence/` and `public/longdo-spike.html` remain
untracked for the reasons already recorded above. No push was performed; commits
are left for manual review.

---

## Thursday — 2026-09-03

### Longdo Map Native Control Cleanup

Commit: `f403917`

`feat(cms): hide the redundant native controls on the Longdo map`

`longdo/longdoUi.ts` (new `hideRedundantLongdoUi`, deferred to the map's `ready`
event because v3 wires `map.Ui` asynchronously), the call site and the
`top-14` → `top-2` toolbar-row move in `LongdoAddressMap.tsx`, and the `map.Ui`
typing (`LongdoUiControl`, `Partial<Record<...>>`) in `longdoApi.ts`. `tsc -b`
passes cleanly on the tree.

ArcGIS stays the default and no environment selects Longdo, so `src/changelog.json`
carries no entry and there is no version bump — same reasoning as `1d32ce9` /
`fc25585`. These three files were edited late Wednesday, after that day's
documentation run, and are recorded and committed here in the Thursday batch.

### Searchable Select Dropdown Layering Fix

Commit: `06f54aa`

`fix(cms): keep the searchable select dropdown above nearby content`

`SearchSelectInput.tsx` (`z-10` → `z-50` on the open panel of `SearchableSelect`
and `SearchableSelectApi`), plus the `src/changelog.json` 0.67.1 PATCH entry and
the `package.json` bump in the same commit.

### Weekly Documentation Records

`docs: update weekly development records`

`docs/work-log/2026-W36.md` and `docs/commit-log/2026-W36.md` only.

---

**Untracked, deliberately left so (2026-09-03):** `docs/screen-capture/longdo-20260903-1000.png`
and `docs/screen-capture/maptiler-20260903-1000.png` (render verification shots),
`Claude outputs/CAD-FE-Case-Incident-Pin-Radius-ServiceCenter.md` (a scratch
working note). `docs/map-provider-decision-memo*.docx`, `docs/MASTER PROMPT —
Personal Engineering Evidence Manager.md`, `docs/task-evidence/` and
`public/longdo-spike.html` remain untracked for the reasons already recorded
above. No push was performed; commits are left for manual review.

---

## Friday — 2026-09-04

### Officer Assignment Picker — Workload, Assigned Cases and ETA Columns

Commit: `8c87fc0`

`feat(cms): show workload, assigned cases and ETA on the officer picker`

`singleAssignOfficer.tsx` (7-column grid, the `all` / `recommend` view toggle and
the workload-then-assigned-cases ranking), the new
`assignOfficer/workload/` module (`OfficerWorkloadCell`, `OfficerAssignedCasesCell`,
`OfficerEtaTtlCell`, `useUnitWorkloads`, `useOfficerRouteSolves`,
`unitWorkloadStub`), the `POST /units/workload` RTK Query endpoint
(`store/api/unitWorkload.ts`) and its contract types (`types/unitWorkload.ts`),
plus the five new `assign_officer_modal` label / error strings in
`public/i18n/*.json` (staged as their own hunk with `git add -p` so the
`service_center_auto` string in the same files stayed with the map commit).
`tsc -b` passes cleanly on the tree.

No `src/changelog.json` entry and no version bump: `POST /units/workload` does not
exist server-side, so in a real environment the workload and assigned-cases cells
show a retry state and only the on-demand ETA/TTL solve returns real data.
Same reasoning as the map-provider commits (`1d32ce9` / `fc25585`) — the feature
is present but not yet backed by live data.

### Incident Location to Service Center Auto-Match

Commit: `f292cdd`

`feat(cms): match the incident location to a service center on the case map`

The pure helpers (`utils/pointInPolygon.ts`, `utils/incidentRadius.ts`, both with
`*.test.ts`), the match rule (`formFields/serviceCenterMatch.ts` + test,
`useServiceCenterMatch.ts`, `useOrgIncidentRadiusMeters.ts`), the per-provider
radius overlays (`map/incidentRadius/`, `map/longdo/incidentRadius/`,
`map/maptiler/incidentRadius/`), the `incidentRadius` prop threaded through
`mapTypes.ts`, `AddressMapField.tsx`, `ArcgisAddressMap.tsx`,
`LongdoAddressMap.tsx`, `MapTilerAddressMap.tsx`, `BoundaryMapField.tsx` and
`staff/CaseStaffMapField.tsx`, the create-flow wiring in `caseFormFields.tsx`
(auto-lock via the new `autoLockedArea` capability) and `CaseAreaSelect.tsx` /
`CaseLocationSection.tsx` / `capabilities.ts` / `index.ts`, the read-only draw on
`CaseDisplay.tsx`, the `getOrgSettings` endpoint + `OrgSettings` type
(`core/store/api/organizationApi.ts`, `core/types/organization.ts`), the
`service_center_auto` string in `public/i18n/*.json` (the first hunk, split from
the officer-picker strings), and `tsconfig.app.json` (`exclude` `*.test.ts` /
`*.test.tsx` so the new tests stay out of `tsc -b`). `tsc -b` passes cleanly on
the tree.

This one **does** carry a changelog entry (see the release commit below): the
match runs against real district-polygon data already in the app and the
auto-select / lock is user-visible today. Only the org-configurable radius is a
forward contract, and it falls back to 900m.

### Changelog Entry for the Service Center Auto-Match

Commit: `e095025`

`chore(release): publish the service center auto-match as 0.68.0`

`src/changelog.json` + `package.json` bumped 0.67.1 → 0.68.0. MINOR: the
automatic service-center selection on case creation is a new capability, not only
a fix. The officer-picker commit is deliberately **not** part of this release —
it has no user-visible data in a real environment yet.

### Weekly Documentation Records

`docs: update weekly development records`

`docs/work-log/2026-W36.md` and `docs/commit-log/2026-W36.md` only.

---

**Untracked, deliberately left so (2026-09-04):** `Claude outputs/`,
`docs/screen-capture/`, `docs/task-evidence/`, `docs/map-provider-decision-memo*.docx`,
`docs/MASTER PROMPT — Personal Engineering Evidence Manager.md` and
`public/longdo-spike.html` — personal engineering-evidence material, binary
verification artifacts and a scratch SDK probe page, all left for the developer
to include or not, exactly as the earlier notes this week did. No push was
performed; commits are left for manual review.
