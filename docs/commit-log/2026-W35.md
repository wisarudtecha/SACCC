# Commit Log — Week 35, 2026

## Monday — 2026-08-24

### Add Area Templates and Improve Area Management

Commit: `99dbef4`

`feat: add area templates`

### Consolidated Work Log for Area Templates Session

Commit: `4a89249`

`docs: add consolidated work log`

### Package Version Sync for Area Templates Release

Commit: `21600e4`

`chore(release): sync package version to 0.53.0`

### KMS Article Category Browser

Commit: `0a5bac7`

`feat(kms): browse knowledge base articles by category`

### Article Editor Image Drag-and-Reposition & Knowledge Card Refinements

Commit: `d8da71e`

`feat(kms): let article pictures be dragged into place while writing`

### Weekly Documentation Records

Commit: `2ea4a18`

`docs: update weekly development records`

### Editable Area Boundaries on Organization Areas

Commit: `8374d6f`

`feat(cms): edit area boundaries and shape details directly on org areas`

### User Area Assignment Tree Fix

Commit: `664695f`

`fix(core): correct user area assignment when countries share a province code`

---

**Verification note (2026-08-24):** Today's committed scope spans three separate
sessions — the area-templates feature, the KMS article work, and this round's
org-area geometry editing plus the user area-assignment fix. The combined
estimated effort across all work logged for the day is roughly 22 hr, well
beyond the standard 8 hr/day capacity. Work logs are recorded against the actual
scope of what was committed on the date, not time spent by a single person; per
policy each day's headline effort is still capped at 8 hr. No push was performed;
commits are left for manual review.

---

## Tuesday — 2026-08-25

> Note: this code was authored late on 2026-08-24 (files modified 22:50–22:58)
> but was still uncommitted. It was committed in the 2026-08-25 run (shortly
> after midnight) as the outstanding working-tree delta.

### Reliable Org Area Editing & Inactive Area Management

Commit: `866a8d7`

`feat(cms): edit org areas from the authoritative record and manage inactive areas`

### Area Template Tree Preview & Template Action Authorization

Commit: `dd75f83`

`feat(cms): preview the cached area template tree and gate template actions by permission`

### Weekly Documentation Records

Commit: `d6054c0`

`docs: update weekly development records`

---

**Verification note (2026-08-25, post-midnight session):** Both feature commits were verified with
`tsc -b` (commit `866a8d7` type-checked in isolation via stash; the full tree
after `dd75f83` also passed). The two work items total 8 hr of estimated effort
for the reporting day. `src/changelog.json` was updated (0.57.0, 0.58.0);
`src/cms/changelog.json` was not touched. No push was performed; commits are left
for manual review.

---

> Note: the entries below are the daytime session of 2026-08-25 (files modified
> 07:24–17:21), separate from the two feature commits above, which were created
> shortly after midnight on the same date.

### ArcGIS Map Load Cancellation Noise

Commit: `4566f3e`

`fix(cms): suppress AbortError noise from cancelled ArcGIS map loads`

### Conclusive API Failure Handling

Commit: `6a234ee`

`fix(core): treat a conclusive API failure envelope as a real error`

### Case Map Boundaries from Organization Area Data

Commit: `ec51ca5`

`feat(cms): draw case map boundaries from the organization's own areas`

### Ungenerated Area Tree Handling

Commit: `e7b6036`

`fix(cms): tell an ungenerated area tree apart from a failed one`

### Case Location Readout on Expanded Map

Commit: `c7baa13`

`feat(cms): show the case address and coordinates on the expanded map`

### Article Browser Translation Completion

Commit: `bd896b0`

`chore(kms): complete the translations for the article browser`

### API Envelope Contract and Anti-Regression Skill

Commit: `681cbe4`

`docs: record the API envelope contract and add the anti-regression skill`

### Weekly Documentation Records

`docs: update weekly development records`

---

**Verification note (2026-08-25, daytime session):** commits are ordered so that
each one type-checks on its own — the shared envelope/tree guards land first,
the boundary source that consumes them second, and the Area Management screen
that consumes the boundary source third. The full tree passes `tsc -b`.
`src/changelog.json` was updated (0.58.1, 0.59.0, 0.59.1, 0.60.0, 0.60.1) and
`src/cms/changelog.json` was not touched.

The three shared i18n catalogues carried keys belonging to three unrelated work
items, so they were split hunk-by-hunk across the matching commits rather than
committed as one mixed change.

`4566f3e` was created earlier the same day, outside this run; it is recorded
here because it had not yet been documented.

The daytime session totals 8 hr of estimated effort. Combined with the 8 hr
recorded for the post-midnight session, the day's recorded scope is roughly
16 hr — beyond the standard 8 hr/day capacity. Work logs are recorded against
the actual scope of what was committed on the date, not time spent by a single
person; per policy each session's headline effort is still capped at 8 hr.

No push was performed; commits are left for manual review.

---

## Wednesday — 2026-08-26

### Organization Hierarchy In-Place Refresh

Commit: `da02441`

`feat(core): update the organization structure in place after saving`

### Service Hierarchy In-Place Refresh

Commit: `7911431`

`feat(cms): update the service list in place after saving`

### Area Management Form and Tree Refresh Rework

Commit: `326012e`

`feat(cms): rework the area form and keep the tree mounted while it refreshes`

### Customer PII Access by Data Category

Commit: `678e6c6`

`feat(core): resolve PII access per data class and protect the write path`

### Weekly Documentation Records

`docs: update weekly development records`

---

**Verification note (2026-08-26):** commits are ordered core-before-consumers so
each builds on the one before it — the shared `HierarchyView` reveal mechanism
lands with the organization screen, the service screen consumes it next, and the
area screen third. The full tree passes `tsc -b`.

`src/changelog.json` was updated (0.61.0, 0.62.0, 0.63.0, 0.64.0) and
`src/cms/changelog.json` was not touched.

The three shared i18n catalogues again carried keys belonging to two unrelated
work items, so they were split hunk-by-hunk — the area form/confirmation keys
into `326012e`, the personal-data category keys into `678e6c6`.

`src/core/pages/Admin/UserManagement.tsx` was changed as part of the area commit
rather than as separate work: the new `useOrgAreaTrees()` signature left that
call site failing `tsc -b`, so it is the same change finished.

**Not committed:** thirteen files under `src/kms/` (article list, detail,
review, category screens and their components) appeared in the working tree at
17:52 today, after this session had already taken its inventory of the day's
changes. They are unrelated to the four work items above and their state could
not be confirmed as complete, so they were deliberately left uncommitted for the
developer to review and commit separately.

No push was performed; commits are left for manual review.

---

## Thursday — 2026-08-27

### Editable Unit Property Assignment

Commit: `e42aac0`

`feat(cms): let administrators assign properties to a unit`

### Boundary Drawing on the Area Map

Commit: `9daa459`

`feat(cms): draw an area boundary on the map`

### Weekly Documentation Records

`docs: update weekly development records`

---

**Verification note (2026-08-27):** the two feature commits are independent and
were ordered by when the work was done. Each carries its own `src/changelog.json`
entry and `package.json` bump (0.65.0 and 0.66.0), following the same convention
as the rest of the week. `src/cms/changelog.json` was not touched.

The shared i18n catalogues carried keys for both work items at once, so they were
split hunk-by-hunk: the unit property keys went with the first commit and the
boundary geometry keys with the second. The full tree passes `tsc -b`.

The KMS article-browser changes still in the working tree were last modified on
2026-08-26 and are not part of today's work. They were left uncommitted again, as
noted in Wednesday's entry, for the developer to review separately.

No push was performed; commits are left for manual review.
