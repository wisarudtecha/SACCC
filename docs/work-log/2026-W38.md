# Work Log — Week 38, 2026

## Monday — 2026-09-14

### Case Summary by Area Dashboard

**Spending Hour:** 5 hr.

**Activity**
- เพิ่มหน้าดashboard ใหม่ "Case Summary by Area" เข้าไปในเมนู Dashboard ของระบบ CMS แสดงจำนวนเคสแยกตามพื้นที่ (เขต/อำเภอ) พร้อม breakdown ตามหมวดหมู่และสถานะ (new / in progress / complete) รวมทั้งแถวสรุปยอดรวม
- ต่อสาย widget source ใหม่ (`case-summary-by-area`) เข้ากับ custom-dashboard framework ที่มีอยู่แล้ว โดยเพิ่ม type, parser และลงทะเบียนใน registry ให้ข้อมูลที่ส่งมาทาง WebSocket ถูกแปลงเป็นโครงสร้างที่หน้าจอใช้ได้ทันที และอัปเดตแบบ real-time เหมือน dashboard อื่นในชุดเดียวกัน
- เพิ่มคีย์ i18n ที่เกี่ยวข้องครบทั้งสามภาษา (en/th/cn) สำหรับชื่อเมนู หัวตาราง และป้ายกำกับต่าง ๆ ของหน้าใหม่
- ระหว่างเพิ่มเมนูใหม่ในแถบข้าง (sidebar) พบว่าลืมเพิ่มรายการเดียวกันในเมนู "More" ของแถบบน (topbar) ซึ่งผู้ใช้เคยแจ้งว่าเป็นความผิดพลาดที่เกิดซ้ำ จึงตรวจสอบทั้งสองไฟล์เทียบกันและพบช่องว่างเดิมอีกจุดหนึ่ง (เมนู Area Templates) จึงแก้ไขให้ครบทั้งสองจุดในคราวเดียว และบันทึกเป็นบทเรียนไว้ใน anti-regression skill เพื่อกันไม่ให้เกิดซ้ำอีก

**Scope / Context**
- อยู่ในโมดูล CMS ส่วน Dashboard ซึ่งมีชุด dashboard แบบ real-time อยู่แล้ว (Analytics, Callcenter, Service, Custom) งานนี้เป็นการเพิ่ม dashboard ตัวใหม่เข้าชุดเดียวกันโดยใช้โครงสร้าง custom-dashboard source ที่มีอยู่แล้วเป็นฐาน ไม่ได้สร้างระบบดึงข้อมูลใหม่
- ข้อมูลที่แสดงยังอ้างอิงจาก mock data (`caseSummaryByArea.json`) เนื่องจากยังไม่ได้ต่อกับ backend จริงสำหรับ payload ประเภท `CASE-DAILY-SUMMARY-AREA` ในสภาพแวดล้อมนี้
- ปัญหาการซิงก์เมนูระหว่าง sidebar กับ topbar เป็นปัญหาเชิงโครงสร้าง (สองไฟล์ดูแลแยกกันโดยไม่มี source of truth ร่วม) ไม่ใช่ปัญหาเฉพาะจุดของฟีเจอร์นี้ จึงบันทึกเป็นกฎป้องกันสำหรับงานเพิ่มเมนูในอนาคตด้วย

**Outcome**
- ผู้ใช้ที่มีสิทธิ์ `dashboard.view` เข้าถึงหน้า Case Summary by Area ได้ทั้งจาก sidebar และจากเมนู "More" บนแถบบน ครบทั้งสองทาง
- เมนู Area Templates ที่เคยหายไปจากแถบบนก็ปรากฏครบแล้วเช่นกัน ซึ่งเป็นผลพลอยได้จากการตรวจสอบรอบนี้
- `tsc -b` ผ่านทั้งก้อนหลังรวมการเปลี่ยนแปลงทั้งหมด

---

### CLAUDE.md Documentation Accuracy Refresh

**Spending Hour:** 1 hr.

**Activity**
- ปรับปรุงเนื้อหาใน `CLAUDE.md` หลายจุดให้ตรงกับสภาพโค้ดจริงในปัจจุบัน: เพิ่มรายละเอียดผู้ให้บริการแผนที่สามเจ้าที่สลับกันได้ (ArcGIS / MapLibre+MapTiler / Longdo), แก้คำอธิบายโมดูล `kms` จาก "stub ว่างเปล่า" เป็น "ระบบ Knowledge Management ที่พัฒนาเสร็จสมบูรณ์แล้ว" พร้อมระบุ stack ที่ต่างจากโมดูลอื่น, บันทึกว่าคำสั่ง build เพิ่มขนาด Node heap แล้ว และอธิบายเหตุผลที่ปล่อยไลบรารีแผนที่บางตัวไม่ให้เข้า vendor chunk

**Scope / Context**
- เป็นงานตรวจทานเอกสารประจำโปรเจกต์ ไม่ผูกกับฟีเจอร์ใดฟีเจอร์หนึ่งโดยตรง เกิดจากการพบว่าเนื้อหาบางส่วนใน `CLAUDE.md` ล้าสมัยเมื่อเทียบกับโค้ดที่มีอยู่จริง (เช่น `kms` ที่จริงมีไฟล์อยู่ราว 130 ไฟล์แต่เอกสารยังระบุว่าเป็น stub)

**Outcome**
- เอกสารสำหรับผู้ช่วยพัฒนา (Claude Code) และทีมสอดคล้องกับโค้ดจริงมากขึ้น ลดโอกาสให้คำแนะนำผิดพลาดจากข้อมูลเก่าในรอบทำงานถัดไป

**Daily Estimated Effort:** 6 hr. / 8 hr.

> หมายเหตุ: ยังไม่มี commit ใดในวันนี้ก่อนเริ่มรอบบันทึกนี้ (commit ล่าสุดคือ 2026-09-11) การเปลี่ยนแปลงทั้งหมดข้างต้นเป็น uncommitted working-tree changes ที่ยืนยันเวลาแก้ไขจาก mtime ของไฟล์ว่าเป็นวันนี้ (2026-09-14) ทั้งหมด ไม่ได้ปัดชั่วโมงเพิ่มให้ครบ 8 ชม. เนื่องจากขอบเขตงานจริงมีเพียงสองรายการนี้
>
> **Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
> `docs/task-evidence/`, `docs/tickets/`, `docs/business-requirement/`,
> `docs/map-provider-decision-memo*.docx`,
> `docs/MASTER PROMPT — Personal Engineering Evidence Manager.md`,
> `public/longdo-spike.html` and `src/core/mocks/permissionCURL.sh` — personal
> engineering-evidence material, business-requirement drafts, and a scratch
> reference/probe file, left for the developer to include or not, consistent
> with prior weeks. No push was performed; commits are left for manual review.

## Tuesday — 2026-09-15

### Compact Dashboard Widget Tiles and Top Districts Widget

**Spending Hour:** 2.5 hr.

**Activity**
- เพิ่มโหมดแสดงผลแบบ "compact tile" ให้กับ widget บน custom dashboard framework เดิม โดยผู้ใช้สามารถสลับ widget ใด ๆ ให้แสดงเป็นการ์ดไอคอน+ตัวเลขเดียวแทนกราฟ/ตารางแบบเต็มได้ ผ่านตัวเลือก Display ใน config modal ของ widget
- ปรับโครงสร้าง registry และ types ของ custom dashboard ให้รองรับ widget สองแบบ: widget ที่ดึงข้อมูลผ่าน WebSocket source เดิม (sourced) และ widget ที่ดึงข้อมูลเอง (self-fetching) เพื่อเป็นฐานให้ widget ของ Dispatch และแผนที่ Case Summary by Area ที่ทำต่อในวันเดียวกัน
- เพิ่ม widget ใหม่ "Top Districts by Cases" จัดอันดับพื้นที่ตามจำนวนเคส พร้อมกำหนดจำนวนอันดับที่แสดงได้ (topN)
- เขียน unit test ให้ compact adapter ของแต่ละ widget

**Scope / Context**
- เป็นงานปรับโครงสร้างพื้นฐาน (core) ของ custom-dashboard widget framework ที่ widget กลุ่ม Case และ Product ใช้ร่วมกันอยู่แล้ว ทำเพื่อรองรับ requirement ใหม่จาก Dispatch Dashboard และแผนที่ของ Case Summary by Area ที่วางแผนทำในวันเดียวกัน
- ออกแบบให้ `core` ไม่ต้อง import โค้ดของ module อื่น (เช่น `cms`) โดยตรง ใช้วิธีให้แต่ละ module ลงทะเบียน widget definition ของตัวเองแทน ตามแนวทางเดิมที่ระบุไว้ใน CLAUDE.md ว่า core ไม่ควรพึ่งพา cms เพิ่มเติม

**Outcome**
- Widget เดิมทั้งหมดในกลุ่ม Case และ Product สลับไปแสดงแบบ compact tile ได้แล้วผ่าน config เดิม โดยไม่กระทบการแสดงผลปกติ
- มี widget ใหม่สำหรับดูอันดับพื้นที่ที่มีเคสมากที่สุดให้เลือกใช้ในหน้า custom dashboard
- โครงสร้าง registry รองรับ widget แบบ self-fetching แล้ว เป็นฐานให้ Dispatch Dashboard และแผนที่ Case Summary by Area ใช้งานต่อได้ในวันเดียวกัน

---

### Case Summary by Area Map View and Table UX Improvements

**Spending Hour:** 3 hr.

**Activity**
- เพิ่มมุมมองแผนที่ (Map) ให้กับหน้า Case Summary by Area ที่เดิมมีแค่มุมมองตาราง โดยแสดงจำนวนเคสต่อพื้นที่เป็น choropleth map (ไล่สีตามความหนาแน่น) พร้อม legend และข้อความแจ้งเตือนเมื่อ environment ไม่มีข้อมูลขอบเขต (boundary) ให้จับคู่กับพื้นที่
- รองรับผู้ให้บริการแผนที่ทั้งสามตัวที่ระบบมีอยู่แล้ว (ArcGIS, MapLibre+MapTiler, Longdo) ให้มุมมองแผนที่นี้ทำงานเหมือนกันไม่ว่าจะตั้งค่า provider ใด
- เพิ่ม widget "Case Heatmap by Area" เข้า custom dashboard widget library โดยใช้โครงสร้าง self-fetching widget ที่เพิ่งทำเสร็จในงานก่อนหน้า
- ปรับหัวตารางของมุมมอง Table ให้ sticky (ค้างอยู่ด้านบนขณะเลื่อน) รวมถึงแถวสรุปยอดรวม เพื่อให้ยังเห็นหัวคอลัมน์เวลาดูรายชื่อพื้นที่ยาว ๆ

**Scope / Context**
- ต่อยอดจาก dashboard "Case Summary by Area" ที่เพิ่งเปิดใช้เมื่อวันก่อน (0.75.0) เพิ่ม UX ให้ครบทั้งมุมมองตารางและแผนที่
- ใช้โครงสร้าง choropleth ที่มีอยู่แล้วจากงานแผนที่ก่อนหน้านี้ (map provider abstraction) มาปรับใช้กับชุดข้อมูลของ dashboard นี้โดยเฉพาะ

**Outcome**
- ผู้ใช้ดูข้อมูลเคสแยกตามพื้นที่ได้ทั้งแบบตารางและแบบแผนที่ในหน้าเดียว สลับ tab ได้ทันที
- หัวตารางไม่หายไปเวลาต้องเลื่อนดูพื้นที่จำนวนมาก ช่วยให้อ่านข้อมูลง่ายขึ้น
- มี widget แผนที่ความหนาแน่นเคสให้เลือกใช้เพิ่มในหน้า custom dashboard อื่น ๆ ได้ด้วย

---

### Dispatch Dashboard

**Spending Hour:** 2.5 hr.

**Activity**
- สร้างหน้า Dispatch Dashboard ใหม่ทั้งหน้า แสดง KPI สรุปงานสั่งการ, panel สถานะเจ้าหน้าที่ภาคสนามแบบเรียลไทม์, feed เหตุการณ์ล่าสุดของเคส และแผนที่ความหนาแน่นของเหตุการณ์ (incident heatmap)
- เพิ่ม widget ใหม่สามตัว (Officer Status, Events Feed, Incident Heatmap) เข้า custom dashboard widget library โดยใช้โครงสร้าง self-fetching widget เดียวกับ heatmap ของ Case Summary by Area เพื่อให้นำไปวางในหน้า custom dashboard อื่นได้ด้วย
- เพิ่มเมนู Dispatch Dashboard ทั้งใน sidebar และ topbar "More" menu ให้ตรงกันทั้งสองจุด ตามบทเรียนเรื่อง sidebar/topbar parity ที่เคยบันทึกไว้ก่อนหน้านี้
- ผูก route ใหม่ `/cms/dashboard/dispatch` เข้ากับระบบ permission เดิม (`dashboard.view`)

**Scope / Context**
- เป็น dashboard ใหม่สำหรับทีมปฏิบัติการภาคสนาม (dispatch/CAD) ต่อยอดจากการปรับโครงสร้าง custom-dashboard framework ในงานแรกของวันนี้
- ฟีเจอร์ toggle ความหนาแน่นตำแหน่งเจ้าหน้าที่บนแผนที่ยังปิดใช้งานไว้ก่อน (disabled พร้อม tooltip อธิบาย) เนื่องจากรอ backend ส่ง feed ตำแหน่งเจ้าหน้าที่ทั้งองค์กรมาให้ก่อน

**Outcome**
- ทีม dispatch มีหน้าจอภาพรวมสำหรับติดตามสถานะเจ้าหน้าที่ เหตุการณ์ และความหนาแน่นของเหตุการณ์แบบเรียลไทม์ในหน้าเดียว
- widget ของ dispatch นำไปประกอบ dashboard อื่นได้ด้วยผ่าน custom dashboard widget library
- `tsc -b` ผ่าน และ `pnpm test` (vitest) ผ่านครบทั้ง 75 เทส หลังรวมงานทั้งหมดของวันนี้

**Daily Estimated Effort:** 8 hr. / 8 hr.

> **Note on scope:** No commits existed for 2026-09-15 before this run. All
> three feature commits above package pre-existing uncommitted working-tree
> changes, attributed to today based on file mtimes (12:23-17:17). Three
> customer-profile files (`CustomerCreate.tsx`, `CustomerView.tsx`,
> `SocialAccountManager.tsx`) and `src/core/mocks/permissionCURL.sh` were
> modified 2026-09-14 (yesterday, right after the last prior commit) and are
> intentionally excluded from today's commits and this log.
>
> **Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
> `docs/task-evidence/`, `docs/tickets/`, `docs/business-requirement/`,
> `docs/map-provider-decision-memo*.docx`,
> `docs/MASTER PROMPT — Personal Engineering Evidence Manager.md` and
> `public/longdo-spike.html` — personal engineering-evidence material and
> business-requirement drafts, left for the developer to include or not,
> consistent with prior weeks. No push was performed; commits are left for
> manual review.

## Wednesday — 2026-09-16

### Case-Assignment Map Area Scoping and Service Center Retry

**Spending Hour:** 4 hr.

**Activity**
- ปรับแผนที่ในหน้ามอบหมายเคส/แก้ไขเคสให้เริ่มต้นซ่อนทุก layer ขอบเขตพื้นที่ไว้ก่อน แทนที่จะเปิด layer แรกให้อัตโนมัติเหมือนเดิม
- เพิ่มการจำกัดขอบเขตพื้นที่ที่แสดงในตัวเลือก boundary ให้เหลือเฉพาะเขต (district) ที่เจ้าหน้าที่ dispatcher คนนั้นได้รับมอบหมายให้รับผิดชอบ (พร้อม province/country ที่เกี่ยวข้อง) โดยอ่านรายชื่อเขตที่ authorized มาจากข้อมูลโปรไฟล์ผู้ใช้งาน
- คง behavior เดิมไว้สำหรับ Service Center ที่ระบบจับคู่ได้อัตโนมัติ ให้ยังคงแสดงเขตนั้นบนแผนที่แม้ว่า layer อื่นจะถูกซ่อนไว้ทั้งหมด
- เพิ่มปุ่ม "ลองอีกครั้ง" (Try Again) สำหรับกรณีตำแหน่งเหตุการณ์ไม่สามารถจับคู่กับพื้นที่ Service Center เดียวได้ชัดเจน โดยเรียก resolver แบบ stub (ยังไม่มี backend endpoint จริง) พร้อม cooldown 10 วินาทีต่อการกด และ reset สถานะใหม่ทุกครั้งที่ตำแหน่งหมุดเปลี่ยน
- เพิ่ม unit test ยืนยันว่าการจับคู่ Service Center ไม่ขึ้นกับสถานะการแสดงผล boundary layer

**Scope / Context**
- ต่อยอดจากฟีเจอร์ auto-match Service Center เดิม เพื่อรองรับ workflow ของทีม dispatch ที่ต้องการเห็นเฉพาะพื้นที่ในความรับผิดชอบของตนเอง ลดโอกาสเลือกเขตผิดเมื่อมอบหมายเคส
- ปุ่ม "ลองอีกครั้ง" เป็นทางเลือกสำรองสำหรับกรณีระบบจับคู่พื้นที่ไม่ได้ (no-match) ปัจจุบันยังไม่มี backend รองรับจริง จึงทำเป็น stub ไว้ก่อนตามรูปแบบเดียวกับ assign-picker workload endpoint ที่เคยทำไว้ก่อนหน้านี้
- เพิ่มคีย์แปลภาษาใหม่ (ปุ่ม "ลองอีกครั้ง" และข้อความ "กรุณารอสักครู่") ครบทั้งสามภาษา

**Outcome**
- เจ้าหน้าที่ dispatch เห็นเฉพาะเขตพื้นที่ในความรับผิดชอบของตนเองบนแผนที่มอบหมายเคส ลดความสับสนเมื่อเลือกพื้นที่ด้วยตนเอง
- กรณีระบบจับคู่ Service Center ไม่ได้ ผู้ใช้งานมีทางเลือกให้ระบบลองจับคู่ให้อีกครั้งแทนที่จะต้องเลือกเองอย่างเดียว
- `tsc -b` ผ่าน และ `pnpm test` ผ่านครบทุกเทส

---

### Case Map Layer Controls: Dropdown Consolidation

**Spending Hour:** 1.5 hr.

**Activity**
- ปรับปุ่มควบคุม layer "Place" และ "Device" บนแผนที่เคส จากแถวปุ่มแบบ segmented ที่ขยายกว้างขึ้นเรื่อยๆ ตามจำนวนหมวดหมู่ ให้เป็น dropdown แบบรายการเดียวกับเมนู "Map Style" ที่มีอยู่แล้ว
- แยก component ควบคุม dropdown ออกมาเป็นตัวกลางใช้ร่วมกันระหว่าง Place และ Device เพื่อลดโค้ดซ้ำ
- คงปุ่มเปิด/ปิด layer แยกจากปุ่มเปิด dropdown ตามการตัดสินใจของ stakeholder ว่าการเปิดดูรายการหมวดหมู่ต้องไม่ทำให้ layer เปิด/ปิดไปด้วย

**Scope / Context**
- เป็นงานปรับปรุง UI ล้วนๆ ไม่กระทบ logic การทำงานของ layer เดิม เตรียมรองรับการเพิ่มหมวดหมู่ใหม่ในอนาคตโดยไม่ทำให้ toolbar กว้างเกินไป

**Outcome**
- แถบเครื่องมือบนแผนที่มีความกว้างคงที่ไม่ว่าจะมีกี่หมวดหมู่ และรูปแบบการใช้งาน Place/Device สอดคล้องกับเมนูอื่นในแผนที่มากขึ้น

---

### Area Management: Cached Tree Regeneration and Required-Field Markers

**Spending Hour:** 2.5 hr.

**Activity**
- เพิ่มกลไกติดตามว่าประเทศใดมีการแก้ไขข้อมูล (เพิ่ม/แก้ไข/ลบ ประเทศ, จังหวัด, อำเภอ) แต่ cache ของแผนผังพื้นที่ (org country tree) ยังไม่ถูกสร้างใหม่ให้ตรงกัน โดย flag ไว้เป็นสถานะ "dirty" ต่อประเทศ
- เพิ่มปุ่ม "Generate" ต่อแถวในหน้า Area Hierarchy ให้ผู้ดูแลระบบสั่ง regenerate cache ของประเทศนั้นได้ทันที พร้อม highlight ปุ่มเมื่อประเทศนั้นมีการเปลี่ยนแปลงค้างอยู่
- เมื่อผู้ดูแลระบบลบประเทศ เพิ่ม modal แจ้งเตือนให้เลือก regenerate cache ทันที เนื่องจากแถวของประเทศนั้นหายไปจากตารางแล้วจะไม่มีปุ่ม Generate ให้กดอีก
- เพิ่มเครื่องหมายดอกจัน (*) กำกับ field ที่จำเป็นต้องกรอกในฟอร์ม Country/Province/District

**Scope / Context**
- แก้ปัญหาที่ cache ของแผนผังพื้นที่ (ใช้แสดงในหลายจุดของระบบ) ไม่อัปเดตทันทีหลังผู้ดูแลระบบแก้ไขข้อมูลพื้นที่ ทำให้ต้องมีวิธีแจ้งเตือนและสั่ง regenerate เอง เนื่องจากยังไม่มีการ regenerate อัตโนมัติจากฝั่ง backend
- ปรับปรุง UX ของฟอร์มเพิ่ม/แก้ไขพื้นที่ให้ชัดเจนขึ้นว่า field ใดจำเป็นต้องกรอก

**Outcome**
- ผู้ดูแลระบบเห็นได้ชัดเจนว่าประเทศใดมี cache แผนผังพื้นที่ที่ยังไม่อัปเดต และสามารถสั่ง regenerate ได้เองโดยไม่ต้องรอรอบถัดไป
- ลดโอกาสเผลอส่งฟอร์มพื้นที่โดยไม่กรอกข้อมูลที่จำเป็น
- `tsc -b` ผ่าน และ `pnpm test` ผ่านครบทั้ง 76 เทส หลังรวมงานทั้งหมดของวันนี้

**Daily Estimated Effort:** 8 hr. / 8 hr.

> **Note on scope:** No commits existed for 2026-09-16 before this run.
> `docs/task-evidence/` was created today (17:08) but is scratch/reference
> material added during the session, not represented as its own Work Log
> entry. Three customer-profile files (`CustomerCreate.tsx`, `CustomerView.tsx`,
> `SocialAccountManager.tsx`) remain modified since 2026-09-14 and continue
> to be excluded from commits and this log, consistent with prior days.
> `AreaFormModal.tsx` and `areaFormFields.ts` were found modified mid-session
> (saved at 17:38-17:39, after the initial status snapshot) and are included
> above under Area Management as the required-field marker work, since their
> content and timing place them in the same session.
>
> **Untracked, deliberately left so:** `Claude outputs/`, `docs/screen-capture/`,
> `docs/task-evidence/`, `docs/tickets/`, `docs/business-requirement/`,
> `docs/map-provider-decision-memo*.docx`,
> `docs/MASTER PROMPT — Personal Engineering Evidence Manager.md`,
> `public/longdo-spike.html`, `src/cms/mocks/areaGO.txt` and
> `src/core/mocks/permissionCURL.sh` — personal engineering-evidence
> material, a GraphQL schema reference dump, and a permission-testing script,
> left for the developer to include or not. No push was performed; commits
> are left for manual review.
