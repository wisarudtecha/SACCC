# Work Log — Week 35, 2026

## Monday — 2026-08-24

### Add Area Templates and Improve Area Management

**Spending Hour:** 8 hr.

**Activity**
- สำรวจและปรับปรุงการจัดการพื้นที่ เพิ่มหน้าจอสำหรับจัดการแม่แบบพื้นที่ ประเทศ จังหวัด และอำเภอ พร้อมเชื่อมต่อข้อมูลผ่าน REST/GraphQL เพิ่มการจัดการเวอร์ชันแบบฉบับร่างและเผยแพร่ การคัดลอกเวอร์ชัน การนำเข้าและซิงก์แม่แบบ รวมถึงปรับปรุงการค้นหา ตาราง ลำดับชั้นพื้นที่ และเมนูการใช้งาน อัปเดตคำแปลภาษาไทย อังกฤษ และจีน และปรับปรุงไฟล์ข้อมูลตัวอย่างกับยูทิลิตีที่เกี่ยวข้อง

**Scope / Context**
- ครอบคลุมงานในโมดูลการตั้งค่าระบบและการจัดการพื้นที่ ได้แก่ หน้ารายการและรายละเอียดแม่แบบพื้นที่ หน้าจัดการจังหวัดและอำเภอ การแสดงขอบเขตบนแผนที่ การเชื่อมต่อ API และ GraphQL ชนิดข้อมูลและการจัดการผลลัพธ์ API การปรับปรุงหน้าจอจัดการพื้นที่เดิม เมนู เส้นทาง และคำแปล

**Outcome**
- เพิ่มความสามารถให้ผู้ดูแลสร้าง แก้ไข ดูประวัติ และเผยแพร่แม่แบบพื้นที่ได้ โดยแม่แบบที่เผยแพร่แล้วจะไม่ถูกแก้ไขโดยไม่ตั้งใจ สามารถนำแม่แบบที่เผยแพร่ไปสร้างหรือปรับปรุงพื้นที่ขององค์กรได้ ระบบจัดการพื้นที่มีโครงสร้างและการโต้ตอบที่ชัดเจนขึ้น รองรับข้อมูลหลายภาษาและการทำงานผ่าน REST/GraphQL พร้อมอัปเดตบันทึกการเปลี่ยนแปลงเป็นเวอร์ชัน 0.53.0 และตรวจสอบแล้วว่าโปรเจกต์ build สำเร็จ

*(บันทึกจากรอบก่อนหน้าในวันเดียวกัน คอมมิตแล้วก่อนรอบนี้จะเริ่ม — ดู `docs/work-log/2026-08-24.md`)*

---

### KMS Article Category Browser

**Spending Hour:** 3.5 hr.

**Activity**
- เพิ่มหน้าจอใหม่สำหรับเรียกดูบทความในคลังความรู้ (Knowledge Base) โดยแบ่งตามหมวดหมู่ พร้อมแผงต้นไม้หมวดหมู่ด้านข้างและช่องค้นหา
- เชื่อมต่อข้อมูลหมวดหมู่และรายการบทความผ่าน GraphQL พร้อมรองรับการโหลดบทความเพิ่มแบบ infinite scroll (Load more)
- ปรับโครงสร้างเมนู Knowledge ในแถบด้านข้างและเมนู More บนแถบด้านบน แยกหน้าจอ "จัดการบทความ" (สำหรับผู้ดูแล) ออกจากหน้าจอ "บทความ" ใหม่ (สำหรับเรียกดู) อย่างชัดเจน
- ปรับปรุงคำแปลภาษาไทยและอังกฤษของเมนูที่เกี่ยวข้อง

**Scope / Context**
- อยู่ในโมดูล Knowledge Management (KMS) ส่วนของบทความ (Articles) เพื่อให้ผู้ใช้งานทั่วไปเข้าถึงและค้นหาบทความในคลังความรู้ได้สะดวกขึ้นโดยไม่ต้องผ่านหน้าจัดการที่ออกแบบไว้สำหรับผู้ดูแลเนื้อหา
- คำแปลภาษาจีนของป้ายเมนูใหม่ยังไม่ได้อัปเดตในรอบนี้

**Outcome**
- ผู้ใช้งานที่มีสิทธิ์ดูบทความสามารถเข้าหน้าจอใหม่ "บทความ" จากเมนูด้านข้าง เพื่อไล่ดูตามหมวดหมู่และค้นหาบทความได้โดยตรง
- เมนูจัดการบทความ (สำหรับผู้ที่มีสิทธิ์จัดการ) ย้ายไปอยู่ในเมนู More บนแถบด้านบน แยกจากหน้าจอเรียกดูทั่วไป ลดความสับสนระหว่างสิทธิ์ผู้ใช้งานสองกลุ่ม
- ตรวจสอบแล้วว่าโปรเจกต์ผ่าน type check (`tsc -b`) สำเร็จ พร้อมอัปเดตบันทึกการเปลี่ยนแปลงเป็นเวอร์ชัน 0.54.0

---

### Article Editor Image Drag-and-Reposition & Knowledge Card Refinements

**Spending Hour:** 3.5 hr.

**Activity**
- เพิ่มความสามารถลากรูปภาพภายในตัวแก้ไขบทความ (Rich Text Editor) เพื่อย้ายตำแหน่ง รองรับการย้ายเข้า/ออกจากเซลล์ตาราง พร้อมเส้นแสดงตำแหน่งที่จะวางระหว่างลาก
- เพิ่มการรองรับลากไฟล์รูปภาพหรือเนื้อหาจากภายนอกเบราว์เซอร์มาวางลงในตัวแก้ไขได้โดยตรง
- ปรับการแทรกรูปภาพด้วยลิงก์ URL ให้ได้ผลลัพธ์เหมือนกับรูปที่อัปโหลด (จำกัดความกว้างและปิดการลากแบบ native)
- ปรับการ์ดแสดงบทความในคลังความรู้ (ใช้ในหน้าเคสและแดชบอร์ด) ให้โหลดแสดงผลได้มากขึ้นต่อครั้ง และนำปุ่มคัดลอกลิงก์ที่ไม่ได้ใช้งานออก

**Scope / Context**
- อยู่ในโมดูล Knowledge Management ส่วนตัวแก้ไขเนื้อหาบทความ (ArticleCreateForm) และส่วนแสดงการ์ดบทความที่ใช้ร่วมกันหลายหน้าจอ (Case Panel, Knowledge Dashboard)
- เป็นการต่อยอดความสามารถจัดวางรูปภาพในเนื้อหาที่มีอยู่เดิม (resize รูปภาพ, ตาราง) ให้ครบถ้วนขึ้น

**Outcome**
- ผู้เขียนบทความสามารถจัดวางตำแหน่งรูปภาพในเนื้อหาได้สะดวกขึ้นโดยไม่ต้องลบแล้วแทรกใหม่ และสามารถลากไฟล์รูปจากเครื่องมาแทรกได้ทันที
- การ์ดบทความในคลังความรู้แสดงรายการได้มากขึ้นต่อการโหลดหนึ่งครั้ง ลดการต้องกดโหลดเพิ่มบ่อยๆ
- ตรวจสอบแล้วว่าโปรเจกต์ผ่าน type check (`tsc -b`) สำเร็จ พร้อมอัปเดตบันทึกการเปลี่ยนแปลงเป็นเวอร์ชัน 0.55.0

---

### Editable Area Boundaries on Organization Areas

**Spending Hour:** 5 hr.

**Activity**
- เพิ่มความสามารถให้กรอกและแก้ไขขอบเขต (map boundary) ของประเทศ จังหวัด และอำเภอ ได้โดยตรงบนฟอร์มในหน้า Area Management เดิมที่ขอบเขตมาได้ทางเดียวคือผ่านแม่แบบพื้นที่ (area template) เท่านั้น
- เพิ่มฟิลด์ shape area, shape length และ year of data สำหรับระดับประเทศ พร้อมขยายชนิดฟิลด์ของฟอร์ม (AreaFormModal) ให้รองรับ number/textarea และข้อความ hint ใต้ช่อง
- เพิ่ม validation ของค่าขอบเขตก่อนบันทึก เพื่อให้ค่าที่รูปแบบไม่ถูกต้องแจ้งเตือนที่ฟอร์มแทนที่จะไปพลาดตอนบันทึก
- เพิ่มยูทิลิตี `toCoordinatesPayload` ที่ใช้ร่วมกันทั้งฝั่ง org area และ area template เพื่อจัดการความต่างระหว่าง "ไม่ได้แก้" (ส่งค่าขอบเขตเดิมกลับไป ไม่ให้ถูกล้าง) กับ "ล้างค่า" (ส่ง array ว่าง)
- เพิ่ม endpoint สำหรับอ่านข้อมูลรายตัว (country/province/district by id) ทั้งฝั่ง REST และ mapping GraphQL
- แสดงป้ายที่มา (provenance) บนรายการพื้นที่ ระบุว่าประเทศใดถูกสร้างหรือซิงก์มาจากแม่แบบใด เพื่อแยกพื้นที่ที่สร้างเองกับพื้นที่ที่มาจากแม่แบบ
- อัปเดตคำแปลไทย/อังกฤษ/จีน และปรับไฟล์ตัวอย่าง (areaCURL.sh) ให้ตรงกับ payload ใหม่

**Scope / Context**
- อยู่ในโมดูล System Configuration ส่วน Area Management (CMS) ต่อยอดจากงานแม่แบบพื้นที่ก่อนหน้า โดยเปิดให้พื้นที่ขององค์กรมี geometry ของตัวเองได้โดยไม่ต้องผูกกับแม่แบบ
- หมายเหตุการออกแบบ: การส่ง coordinates เป็น array ว่างเพื่อ "ล้าง" ยังเป็นสมมติฐานที่ยังไม่ยืนยันกับ backend จึงจำกัดไว้ในจุดเดียว (`toCoordinatesPayload`) และฟิลด์ postcode ของอำเภอฝั่ง org ยังเป็น read-only ตาม input ของ backend

**Outcome**
- ผู้ดูแลสามารถกำหนด/แก้ไขขอบเขตและข้อมูลรูปทรงของพื้นที่ระดับองค์กรได้เองบนฟอร์ม การแก้ไขที่ไม่แตะขอบเขตจะไม่ทำให้ขอบเขตเดิมหาย และการเว้นว่างถือว่าไม่มีขอบเขต
- รายการพื้นที่แสดงชัดเจนขึ้นว่าประเทศใดมาจากแม่แบบ ช่วยแยกแยะข้อมูลที่สร้างเองกับที่รับมา ตรวจสอบแล้วว่าโปรเจกต์ผ่าน type check (`tsc -b`) สำเร็จ พร้อมอัปเดตบันทึกการเปลี่ยนแปลงเป็นเวอร์ชัน 0.56.0

---

### User Area Assignment Tree Fix

**Spending Hour:** 2 hr.

**Activity**
- ปรับหน้าจอมอบหมายพื้นที่ให้ผู้ใช้ (User Area Assignment) ให้อ่านโครงสร้างประเทศ/จังหวัด/อำเภอแบบซ้อนกัน (nested tree) ขององค์กรโดยตรง แทนการดึงรายการแบบ flat สามชุดแล้วนำมา re-join กันในฝั่ง browser
- แก้บั๊กการจัดกลุ่มผิดเมื่อสองประเทศใช้รหัสจังหวัดซ้ำกัน โดยเพิ่ม countryId เข้าไปในการ cascade เลือกระดับจังหวัด และทำ namespace ให้กับ key การขยาย/ยุบและ React key ตามประเทศ
- ยกเลิกการโหลดรายการจังหวัด/อำเภอทั้งหมดล่วงหน้า (length:10000 / length:20000) ที่ใหญ่เกินความจำเป็นของหน้าจอนี้

**Scope / Context**
- อยู่ในโมดูล core ส่วน User Management แท็บ Area ของผู้ใช้ ปัญหาเดิมคือการติ๊กจังหวัดหรือทั้งประเทศอาจไปติ๊กอำเภอของอีกประเทศที่บังเอิญมีรหัสจังหวัดเดียวกัน และจังหวัดรหัสซ้ำจะขยาย/ยุบพร้อมกัน

**Outcome**
- การมอบหมายพื้นที่ให้ผู้ใช้ถูกต้องแม้มีรหัสจังหวัดซ้ำข้ามประเทศ การขยาย/ยุบและการ cascade เลือกทำงานแยกกันตามประเทศ และลดปริมาณข้อมูลที่โหลดตอนเปิดหน้าจอ ตรวจสอบแล้วว่าผ่าน type check (`tsc -b`) สำเร็จ พร้อมอัปเดตบันทึกการเปลี่ยนแปลงเป็นเวอร์ชัน 0.56.1

**Daily Estimated Effort:** 8 hr. (นับเฉพาะเพดานมาตรฐานต่อวัน) — วันนี้มีงานที่บันทึกไว้หลายรอบ (แม่แบบพื้นที่ 8 hr. + KMS 7 hr. + งานพื้นที่/ผู้ใช้รอบนี้ประมาณ 7 hr.) รวมประมาณ 22 hr. ซึ่งเกินกำลังการทำงานมาตรฐานต่อวันไปมาก เป็นการบันทึกตามขอบเขตงานจริงที่คอมมิตในวันเดียวกัน ไม่ใช่เวลาที่ใช้จริงต่อคน โปรดดูหมายเหตุใน Verification ของ Commit Log ประกอบ

---

## Tuesday — 2026-08-25

> หมายเหตุ: โค้ดชุดนี้เขียนต่อเนื่องช่วงดึกของวันที่ 2026-08-24 (แก้ไขไฟล์เวลา 22:50–22:58) แต่ยังไม่ได้คอมมิต จึงถูกบันทึกและคอมมิตในรอบทำงานของวันที่ 2026-08-25 (หลังเที่ยงคืนเล็กน้อย) เป็น delta ที่ยังค้างอยู่ใน working tree

### Reliable Org Area Editing & Inactive Area Management

**Spending Hour:** 5 hr.

**Activity**
- ปรับ flow การแก้ไขพื้นที่ (ประเทศ/จังหวัด/อำเภอ) ในหน้า Area Management ให้ดึงข้อมูลตัวจริงของ record ตาม id ผ่าน lazy query แทนการ seed ค่าจาก tree cache ซึ่งไม่พกฟิลด์ nameSpace ทำให้การบันทึกเดิมอาจล้างค่าที่ฟอร์มไม่ได้โหลดมา
- เพิ่มฟิลด์ namespace และ toggle สถานะ active/inactive ในฟอร์ม พร้อมเพิ่ม field type "toggle" และสถานะ isLoadingRecord เพื่อ disable ฟอร์มระหว่างโหลด record ป้องกันการแก้/บันทึกทับข้อมูลที่ยังโหลดไม่เสร็จ
- เพิ่มตัวเลือก "แสดงรายการที่ปิดใช้งาน" (show inactive) เพื่อให้เข้าถึงและเปิดใช้งานพื้นที่ที่ถูกปิดไว้ได้อีกครั้ง จากเดิมที่หายไปจากรายการทั้งหมด
- เพิ่มการควบคุมสิทธิ์ปุ่มแก้ไข/ลบ (canUpdate/canDelete) ตาม area.update / area.delete
- ปรับ AreaTemplateSyncModal ให้ preselect แม่แบบต้นทาง (sourceTemplateId) ของประเทศที่เลือกอัตโนมัติ และเพิ่มคำเตือนก่อนใช้โหมด sync ที่เขียนทับข้อมูลเดิม (replace_*) ซึ่งย้อนกลับไม่ได้
- ลดความซับซ้อนของ props โดยตัด setter รายฟิลด์ราว 20 ตัวออกจาก AreaHierarchyView เหลือ callback onEditRecord/onCreateChild และอัปเดตคำแปลไทย/อังกฤษ/จีน

**Scope / Context**
- โมดูล System Configuration ส่วน Area Management (CMS) ต่อยอดจากงานแม่แบบพื้นที่และการแก้ไขขอบเขตพื้นที่ก่อนหน้า ปัญหาหลักที่แก้คือฟอร์มแก้ไขที่ seed จาก tree metadata ทำให้ฟิลด์ที่ tree ไม่ได้พก (เช่น nameSpace) หายไปตอนบันทึก และไม่มีทางเข้าถึงรายการที่ปิดใช้งานเพื่อกู้คืน
- ข้อจำกัดที่ยังคงไว้: การส่ง coordinates เป็น array ว่างเพื่อ "ล้าง" ขอบเขต ยังรวมไว้ที่ `toCoordinatesPayload` จุดเดียวเช่นเดิม

**Outcome**
- การแก้ไขพื้นที่บันทึกครบทุกฟิลด์ตาม record จริง ไม่ล้างค่าที่ไม่ได้แสดงบนฟอร์มโดยไม่ตั้งใจ ผู้ดูแลตั้ง namespace และเปิด/ปิดสถานะพื้นที่ได้ เข้าถึงและกู้คืนพื้นที่ที่ปิดใช้งานได้ ปุ่มแก้ไข/ลบแสดงเฉพาะผู้มีสิทธิ์ และการซิงก์จากแม่แบบเดาต้นทางให้อัตโนมัติพร้อมเตือนก่อนเขียนทับ ตรวจสอบผ่าน `tsc -b` สำเร็จ พร้อม bump เวอร์ชันเป็น 0.57.0

---

### Area Template Tree Preview & Template Action Authorization

**Spending Hour:** 3 hr.

**Activity**
- เพิ่มคอมโพเนนต์ AreaTreePreview (read-only) แสดงโครงสร้าง country/province/district ของ tree ที่แคชไว้ บนหน้า AreaTemplateDetailView โดยดึงผ่าน useGetTemplateCountryTreeQuery ทำให้ปุ่ม "สร้างโครงสร้างใหม่" (regenerate tree) มีผลลัพธ์ที่มองเห็นได้ และผู้เขียนตรวจสอบโครงสร้างก่อนเผยแพร่ได้
- เพิ่มฟิลด์ shapeArea และ shapeLength ในฟอร์มแม่แบบระดับประเทศ ให้สอดคล้องกับฟอร์มพื้นที่ระดับองค์กร
- เพิ่มการควบคุมสิทธิ์การกระทำกับแม่แบบ (แก้ไข/เผยแพร่/fork/ลบ/ซิงก์) ทั้งที่ปุ่ม (ผ่าน prop `permission` ใหม่ใน View action) และภายใน handler (hasPermission/hasAnyPermission) เป็น defense-in-depth และอัปเดตคำแปลของ tree preview

**Scope / Context**
- โมดูล System Configuration ส่วน Area Template (CMS) เดิมปุ่ม regenerate tree รีบิลด์แคชที่ไม่มีส่วนใดของแอปแสดงผล และการกระทำกับแม่แบบยังไม่ถูกจำกัดสิทธิ์ในระดับ handler จึงเสี่ยงถูกเรียกได้หากมีการ refactor UI ในอนาคต

**Outcome**
- ผู้ดูแลเห็นโครงสร้างแม่แบบที่แคชไว้และยืนยันความถูกต้องก่อนเผยแพร่ได้ ฟอร์มแม่แบบประเทศบันทึกข้อมูลรูปทรงเพิ่มได้ และการกระทำกับแม่แบบถูกจำกัดเฉพาะผู้มีสิทธิ์ทั้งที่ UI และ handler ตรวจสอบผ่าน `tsc -b` สำเร็จ พร้อม bump เวอร์ชันเป็น 0.58.0
---

> หมายเหตุ: ตั้งแต่หัวข้อนี้เป็นต้นไปคือรอบทำงานช่วงกลางวันของวันที่ 2026-08-25 (แก้ไขไฟล์เวลา 07:24–17:21) แยกจากสองหัวข้อด้านบนที่คอมมิตไปตั้งแต่ช่วงหลังเที่ยงคืน

### Case Map Boundaries from Organization Area Data

**Spending Hour:** 3.5 hr.

**Activity**
- เปลี่ยนแหล่งข้อมูลขอบเขตการปกครองบนแผนที่เคส จากไฟล์ GeoJSON แบบ static ที่ครอบคลุมเฉพาะกรุงเทพฯ มาเป็นข้อมูลพื้นที่จริงขององค์กรที่ดึงผ่าน API โดยเลือกแหล่งข้อมูลด้วย environment flag และคงชุดไฟล์เดิมไว้เป็นมุมมองอ้างอิงสำหรับแยกปัญหาการวาดออกจากปัญหาข้อมูลฝั่ง backend
- ปรับโครงสร้างการกำหนดค่าระดับพื้นที่ (level table) ให้รองรับสองชุดชั้นข้อมูลที่ไม่เหมือนกัน คือ country → province → district ของข้อมูลองค์กร และ province → district → subdistrict ของไฟล์ตัวอย่าง โดยย้ายทุกอย่างที่ผูกกับชื่อระดับไปไว้ในตารางเดียว ทั้ง label i18n ระดับที่เปิดแสดงเป็นค่าเริ่มต้น น้ำหนักเส้นและสีพื้น รวมถึงลำดับการวาด
- เพิ่มโมดูลคำนวณสีขอบเขตในฝั่ง browser (adjacency four-colouring) เนื่องจาก API พื้นที่ไม่ได้ส่งค่าสีมาด้วย โดย port อัลกอริทึมชุดเดียวกับสคริปต์ที่ใช้ generate ไฟล์ static เพื่อคงคุณสมบัติที่ว่าพื้นที่ที่ติดกันจะไม่ใช้สีเดียวกัน ซึ่งเป็นเงื่อนไขของบันทึกผลตรวจ accessibility ของชุดสีเดิม
- ปรับ layer hook ให้รองรับการโหลดข้อมูลแบบ asynchronous พร้อมจัดการ cancellation ตอน unmount และทำ reference counting ของ URL ข้อมูลชั่วคราว เพื่อไม่ให้ข้อมูลขอบเขตค้างในหน่วยความจำเมื่อมีแผนที่หลายตัวบนหน้าจอเดียวกัน
- กรองพื้นที่ที่ถูกปิดใช้งาน (inactive) ออกจากทั้งแผนที่และรายการตัวเลือก และเพิ่มการล้าง cache ขอบเขตเมื่อมีการแก้ไขข้อมูลพื้นที่ เพื่อให้แผนที่แสดงรูปทรงใหม่โดยไม่ต้อง reload ทั้งหน้า
- อัปเดตคำแปลไทย อังกฤษ และจีน สำหรับชั้นข้อมูลระดับประเทศที่เพิ่มเข้ามา

**Scope / Context**
- อยู่ในโมดูล CMS ส่วนแผนที่ของเคส (case map) ซึ่งเดิมออกแบบ interface ไว้ให้สลับแหล่งข้อมูลได้อยู่แล้ว รอบนี้คือการเติมฝั่ง implementation ที่อ่านจากข้อมูลพื้นที่ขององค์กรจริง ต่อยอดจากงาน Area Management และ Area Template ที่ทำมาก่อนหน้า
- ข้อจำกัดจากฝั่ง backend ที่ยังต้องแก้ปัญหาไว้ในโค้ดและควรแจ้งทีม backend ได้แก่ ข้อมูลพื้นที่ยังไม่มีชื่อภาษาจีน (ใช้ชื่อภาษาอังกฤษแทนไปก่อน) และโครงสร้าง coordinates ปัจจุบันยังไม่รองรับพื้นที่ที่แยกเป็นหลายส่วน เช่น จังหวัดที่มีเกาะ

**Outcome**
- แผนที่เคสแสดงขอบเขตประเทศ จังหวัด และอำเภอ ตามข้อมูลพื้นที่ที่องค์กรตั้งค่าไว้จริง ไม่จำกัดเฉพาะกรุงเทพฯ อีกต่อไป และรองรับองค์กรที่มีหลายประเทศ
- พื้นที่ที่ติดกันแยกออกจากกันได้ด้วยสีเสมอ พื้นที่ที่ปิดใช้งานไม่ปรากฏบนแผนที่ และการแก้ไขพื้นที่มีผลกับแผนที่ทันทีโดยไม่ต้องรีเฟรชหน้า
- การเพิ่มหรือสลับชุดชั้นข้อมูลในอนาคตทำได้จากตารางกำหนดค่าจุดเดียว โดยไม่ต้องแก้ logic ที่กระจายอยู่ใน hook, picker และ toolbar

---

### Conclusive API Failure Handling

**Spending Hour:** 2 hr.

**Activity**
- ปรับชั้นเชื่อมต่อ API (hybrid REST/GraphQL base query) ให้แปลงความล้มเหลวเชิงธุรกิจที่ backend ตอบกลับมาพร้อม HTTP 200 แต่มีสถานะเป็นค่าปฏิเสธ ให้กลายเป็น error จริงของ RTK Query เพื่อให้ฝั่งหน้าจอรับรู้ว่าล้มเหลวตามที่ควรเป็น
- จำกัดการแปลงไว้เฉพาะกรณีที่ระบุได้ชัดเจนเท่านั้น ส่วนกรณีที่สรุปไม่ได้ เช่น backend ไม่ได้ส่งฟิลด์สถานะมาเลย ยังคงให้ผ่านเหมือนเดิม เพื่อไม่ให้ทุก endpoint ที่ไม่ส่งฟิลด์นี้พังพร้อมกัน
- แก้ตัวตรวจสอบผลลัพธ์ฝั่ง CMS ที่เดิมอ่านรหัสความล้มเหลวเป็นสำเร็จ เพราะตรวจเพียงว่ามีค่าหรือไม่ ทำให้ขึ้นข้อความสำเร็จทับการบันทึกที่ถูกปฏิเสธ
- เพิ่ม guard สำหรับตรวจรูปร่างของข้อมูลแทนการตรวจว่ามีค่าหรือไม่ เนื่องจากชั้น normalize แปลง payload ว่างให้เป็น array ว่าง ซึ่งถูกมองว่ามีค่า
- บันทึกสัญญาการทำงานใหม่นี้ไว้ใน CLAUDE.md และเพิ่มเอกสารวิเคราะห์ปัญหาไว้ใน explanation log

**Scope / Context**
- อยู่ในโมดูล core ชั้น API ที่ทุกฟีเจอร์ใช้ร่วมกัน ปัญหาเดิมคือ backend ตอบการปฏิเสธด้วย HTTP 200 ทำให้ฝั่ง frontend มองว่าสำเร็จ กระทบทั้งการบันทึกโน้ตลูกค้า การผูกบัญชี social ของลูกค้า และการอ่านโครงสร้างพื้นที่
- เป็นการรวมศูนย์ logic ที่เดิมกระจายอยู่ตามหน้าจอและตรวจไม่ตรงกัน มาไว้ที่ชั้น API ชั้นเดียว

**Outcome**
- การกระทำที่ถูก backend ปฏิเสธถูกรายงานเป็นความล้มเหลวที่หน้าจอ แทนที่จะขึ้นข้อความสำเร็จแล้วล้างข้อมูลที่ผู้ใช้กรอกทิ้ง
- endpoint ที่ไม่ได้ส่งฟิลด์สถานะยังทำงานเหมือนเดิมทุกประการ ไม่มี regression ในส่วนที่ไม่เกี่ยวข้อง
- มีจุดเดียวที่ตัดสินความสำเร็จหรือล้มเหลวของ API ทำให้หน้าจอที่จะเขียนต่อจากนี้ไม่ต้องตรวจซ้ำเองอีก

---

### Ungenerated Area Tree Handling

**Spending Hour:** 1.5 hr.

**Activity**
- แยกกรณียังไม่ได้สร้างโครงสร้างพื้นที่ ออกจากกรณีโหลดโครงสร้างพื้นที่ไม่สำเร็จ โดยใช้ข้อความที่ backend ส่งกลับมาเป็นตัวจำแนก และรายงานประเทศที่อ่านโครงสร้างไม่ได้ออกมาพร้อมเหตุผล แทนที่จะตัดทิ้งเงียบ
- ปรับหน้า Area Management และหน้า preview โครงสร้างของแม่แบบพื้นที่ให้ใช้ guard ตัวเดียวกัน เพื่อไม่ให้ payload ว่างถูกอ่านเป็นข้อมูลประเทศ ซึ่งเดิมทำให้เกิดแถวเปล่าที่ไม่มีชื่อ แทนข้อความแนะนำให้กดสร้างโครงสร้างก่อน
- เตรียมข้อความแจ้งเตือนบนหน้าจอและปุ่มสร้างโครงสร้างไว้ครบทั้งสามภาษา แต่ยังปิดการแสดงผลไว้ก่อน เนื่องจากยังต้องยืนยันกับ backend ว่าการสั่งสร้างโครงสร้างหลายประเทศพร้อมกันจากหน้าจอนี้ปลอดภัยและไม่ติดเรื่องสิทธิ์

**Scope / Context**
- อยู่ในโมดูล System Configuration ส่วน Area Management และ Area Template ต่อเนื่องจากงานแหล่งข้อมูลขอบเขตแผนที่ เพราะทั้งสองส่วนอ่านโครงสร้างพื้นที่ชุดเดียวกัน
- โครงสร้างพื้นที่เป็น cache ฝั่ง server ที่ต้องสั่งสร้างก่อน ประเทศที่ยังไม่เคยสั่งสร้างจะหายไปจากทั้งรายการพื้นที่ ตัวเลือกพื้นที่ และแผนที่เคส โดยไม่มีอะไรบอกสาเหตุ

**Outcome**
- ระบุได้ว่าประเทศที่หายไปจากรายการเป็นเพราะยังไม่ได้สร้างโครงสร้าง หรือเพราะโหลดไม่สำเร็จจริง
- หน้า preview โครงสร้างของแม่แบบแสดงข้อความแนะนำให้สร้างโครงสร้างก่อนอย่างถูกต้อง ไม่มีแถวเปล่าไร้ชื่ออีก
- ข้อความสำหรับแจ้งเตือนและปุ่มแก้ปัญหาพร้อมเปิดใช้งานได้ทันทีเมื่อยืนยันเงื่อนไขกับ backend เรียบร้อย

---

### Case Location Readout on Expanded Map

**Spending Hour:** 0.5 hr.

**Activity**
- เพิ่มการ์ดแสดงคำอธิบายสถานที่ของเคสพร้อมพิกัด ที่มุมล่างซ้ายของแผนที่เมื่อขยายเต็มจอ และจัดวางร่วมกับข้อความสถานะเดิมของแผนที่โดยไม่ทับกัน
- ส่งค่าคำอธิบายสถานที่จากข้อมูลเคสผ่านชั้นคอมโพเนนต์แผนที่ ทั้งฝั่งฟอร์มสร้างเคส หน้ารายละเอียดเคส และแผนที่เจ้าหน้าที่
- เปิดใช้เฉพาะแผนที่แบบขยายเท่านั้น เนื่องจากแผนที่ขนาดเล็กในฟอร์มไม่มีพื้นที่เพียงพอ

**Scope / Context**
- อยู่ในโมดูล CMS ส่วนแผนที่ของเคส ตอบโจทย์การตรวจสอบว่าจุดบนแผนที่ตรงกับที่อยู่ที่บันทึกไว้หรือไม่ โดยไม่ต้องสลับกลับไปดูฟอร์ม

**Outcome**
- ผู้ใช้งานเทียบตำแหน่งบนแผนที่กับที่อยู่ของเคสได้ในหน้าจอเดียว ทั้งในโหมดสร้างเคสและโหมดดูรายละเอียด

---

### ArcGIS Map Load Cancellation Noise

**Spending Hour:** 0.25 hr.

**Activity**
- กรอง error จากการยกเลิกคำขอออกจากทั้งขั้นตอนโหลด basemap และขั้นตอนเตรียม map view เนื่องจากรอบ mount และ remount ของ React Strict Mode ในโหมด development ยกเลิกคำขอของรอบแรกเสมอ ทำให้ขึ้น error ทั้งที่การ mount รอบสองสำเร็จปกติ
- คงการ log และการ fallback เดิมไว้สำหรับกรณีที่โหลดล้มเหลวจริง และบันทึกผลวิเคราะห์ไว้ใน explanation log

**Scope / Context**
- อยู่ในโมดูล CMS ส่วนแผนที่ของเคส เป็นการลด noise ใน console ระหว่างพัฒนา ที่ทำให้แยก error จริงกับ error ที่ไม่ใช่ปัญหาได้ยาก ไม่กระทบพฤติกรรมบน production

**Outcome**
- console ระหว่างพัฒนาเหลือเฉพาะ error ที่เป็นปัญหาจริง ทำให้ตรวจสอบปัญหาแผนที่ในรอบถัดไปได้เร็วขึ้น

---

### Article Browser Translation Completion

**Spending Hour:** 0.25 hr.

**Activity**
- เติมคำแปลภาษาจีนของเมนูคลังความรู้ที่ค้างไว้จากรอบก่อน โดยแยกเมนูบทความสำหรับเรียกดู ออกจากเมนูจัดการบทความ ให้ตรงกับเมนูภาษาไทยและอังกฤษ
- เติมคำแปลไทยและอังกฤษของหมวดหมู่ ช่องค้นหา สถานะกำลังโหลด และข้อความเมื่อไม่พบบทความ บนหน้าจอเรียกดูบทความ

**Scope / Context**
- อยู่ในโมดูล KMS ส่วนหน้าจอเรียกดูบทความตามหมวดหมู่ ปิดงานคำแปลที่บันทึกไว้ว่ายังค้างอยู่ในรอบวันที่ 2026-08-24

**Outcome**
- เมนูคลังความรู้แยกหน้าจอเรียกดูกับหน้าจอจัดการออกจากกันชัดเจนครบทั้งสามภาษา และหน้าจอเรียกดูบทความไม่มีข้อความที่ยังไม่ได้แปลเหลืออยู่

**Daily Estimated Effort (รอบกลางวัน):** 8 hr.

**Daily Estimated Effort (รวมทั้งวัน):** 8 hr. (นับตามเพดานมาตรฐานต่อวัน) — วันนี้มีสองรอบทำงาน คือรอบหลังเที่ยงคืน 8 hr. และรอบกลางวัน 8 hr. รวมขอบเขตงานที่บันทึกจริงประมาณ 16 hr. ซึ่งเกินกำลังการทำงานมาตรฐานต่อวัน เป็นการบันทึกตามขอบเขตงานที่คอมมิตในวันเดียวกัน ไม่ใช่เวลาที่ใช้จริงต่อคน โปรดดูหมายเหตุใน Verification ของ Commit Log ประกอบ

---

## Wednesday — 2026-08-26

### Customer PII Access by Data Category

**Spending Hour:** 3 hr.

**Activity**
- เปลี่ยนการตรวจสิทธิ์การเข้าถึงข้อมูลส่วนบุคคลของลูกค้า จากสิทธิ์เดียวแบบเห็นทั้งหมด/ไม่เห็นเลย มาเป็นการแยกตามประเภทข้อมูลตาม taxonomy ของฝั่ง backend คือข้อมูลทั่วไป (เบอร์โทร อีเมล เบอร์บ้าน) และข้อมูลอ่อนไหว (เลขบัตรประชาชน วันเกิด รูปถ่าย และที่อยู่ระดับละเอียด) พร้อมประกาศสิทธิ์ฝั่ง case/user ไว้ให้ครบชุดแม้ยังไม่มีหน้าจอที่เรียกใช้
- ปรับ hook การ mask ให้ตอบคำถามเป็นราย field แทนที่จะเป็นราย user และปรับ maskAddress ให้รับ prefix ของที่อยู่ที่กำลัง mask เพื่อให้ที่อยู่ตามทะเบียนและที่อยู่ปัจจุบันตรวจสิทธิ์แยกกันได้
- เพิ่ม flag สำหรับปิดการ mask ฝั่ง frontend ในสภาพแวดล้อมที่ backend ส่งข้อมูลที่ปกปิดมาแล้ว เพื่อไม่ให้ mask ซ้อนกันสองชั้น ซึ่งทำให้ข้อมูลบางรูปแบบ (เช่น วันเกิดที่ถูกปกปิดมาแล้ว) เสียรูปจนแสดงผลผิด
- แยกการป้องกัน write path ออกจาก flag ดังกล่าวอย่างชัดเจน โดยฟิลด์ที่ผู้ใช้ไม่มีสิทธิ์เห็นจะแสดงเป็นข้อความแทน input และถูกตัดออกจาก payload ตอน update รวมถึงข้าม validation ของฟิลด์นั้น และแก้ทางอ้อมที่ displayName เคย fallback ไปใช้อีเมลที่ถูกปกปิด
- เพิ่มตัวเลือกประเภทข้อมูลส่วนบุคคล (ทั่วไป/อ่อนไหว) ในตัวสร้างฟอร์มไดนามิก คู่กับรูปแบบการปกปิดเดิม พร้อม fallback ให้ marker ที่บันทึกไว้ก่อนหน้ายังใช้งานได้ และเติมคำแปลไทย อังกฤษ จีน รวมถึงชุดคำแปลของ formElement ฝั่งจีนที่ยังขาดอยู่ทั้งก้อน

**Scope / Context**
- อยู่ในโมดูล core ส่วน security และโมดูล CMS ส่วนข้อมูลลูกค้า ฟอร์มลูกค้า และฟอร์มไดนามิก รองรับการที่ฝั่ง backend รับหน้าที่ปกปิดข้อมูลส่วนบุคคลเป็นหลักแล้ว โดยฝั่ง frontend เหลือหน้าที่ตัดสินใจเรื่องสิทธิ์การแก้ไขและการแสดงผล
- ประเด็นสำคัญที่ต้องระวังคือฟอร์มลูกค้าประกอบ payload จาก state ไม่ใช่จาก DOM ดังนั้นการซ่อน input เพียงอย่างเดียวไม่ใช่การป้องกัน เพราะค่าที่ถูกปกปิดจะถูกส่งกลับไปทับข้อมูลจริง

**Outcome**
- กำหนดสิทธิ์การเข้าถึงข้อมูลลูกค้าได้ละเอียดขึ้นตามลักษณะงานของผู้ใช้ แทนการเปิดหรือปิดทั้งหมด
- เจ้าหน้าที่ที่ไม่มีสิทธิ์เห็นข้อมูลบางส่วน ยังแก้ไขข้อมูลส่วนอื่นของลูกค้าและบันทึกได้ตามปกติ โดยข้อมูลที่ถูกปกปิดจะไม่ถูกเขียนทับ และหน้าจอสื่อสารชัดเจนว่าฟิลด์นั้นมีอยู่แต่ไม่อยู่ในสิทธิ์ของตน
- ผู้ดูแลระบุประเภทข้อมูลส่วนบุคคลของฟิลด์ในฟอร์มไดนามิกได้เอง ทำให้การควบคุมสิทธิ์ครอบคลุมฟอร์มที่สร้างขึ้นภายหลังด้วย

---

### Area Management Form and Tree Refresh Rework

**Spending Hour:** 3 hr.

**Activity**
- เปลี่ยนแหล่งข้อมูลของผังพื้นที่จาก tree cache ฝั่ง server มาเป็นการอ่าน endpoint รายการประเทศ จังหวัด และอำเภอโดยตรงแล้ว join ฝั่ง client โดย join อำเภอด้วยคู่รหัสประเทศ+จังหวัด ไม่ใช่รหัสจังหวัดเพียงอย่างเดียว เพื่อไม่ให้ข้ามประเทศเมื่อรหัสจังหวัดซ้ำกัน
- ตัดขั้นตอน generate tree และ manual reload ออก เนื่องจาก mutation ของพื้นที่ invalidate tag อยู่แล้ว ทำให้รายการรีเฟรชเองหลังบันทึก และแก้ call site ในหน้า User Management ให้เข้ากับ signature ใหม่ของ hook
- แก้เงื่อนไข loading ที่สลับผังพื้นที่ทั้งก้อนออกทุกครั้งที่ refetch ซึ่งเป็นสาเหตุที่ผังยุบกลับทุกครั้งหลังบันทึก เปลี่ยนเป็นแสดง placeholder เฉพาะตอนยังไม่มีข้อมูล และใช้ข้อความ inline ระหว่างรีเฟรช
- ปรับฟอร์มสร้าง/แก้ไขพื้นที่ให้จัดกลุ่มเป็นข้อมูลระบุตัวตน ขอบเขต และสถานะ พร้อมกำหนดความกว้างของแต่ละฟิลด์ผ่าน descriptor และแยก descriptor ออกเป็นไฟล์ของตัวเอง
- เพิ่มปุ่ม Restore สำหรับคืนค่าฟอร์มกลับเป็น record ที่เปิดมาโดยไม่ต้องยิง API ซ้ำ แยกจากปุ่ม Reset ที่ล้างฟอร์ม
- เพิ่มหน้าต่างยืนยันก่อนบันทึก พร้อมคำเตือนกรณีเปลี่ยนรหัสหรือย้ายพื้นที่แม่ ซึ่งจะทำให้พื้นที่ระดับล่างที่ยังอ้างรหัสเดิมหลุดจากลำดับชั้น และเติมคำแปลทั้งสามภาษา

**Scope / Context**
- อยู่ในโมดูล CMS ส่วน System Configuration หน้า Area Management ต่อเนื่องจากงานแม่แบบพื้นที่และการแก้ไขขอบเขตพื้นที่ในสองวันก่อนหน้า
- ปัญหาหลักที่แก้คือผังพื้นที่ยุบกลับทุกครั้งหลังบันทึก และประเทศที่ยังไม่เคยถูก generate tree จะหายไปจากทุกหน้าจอโดยไม่มีคำอธิบาย
- ข้อจำกัดที่ยังคงอยู่คือแผนที่ของเคสยังอ่านจาก tree cache เดิม จึงยังต้องล้าง cache ของ boundary หลังบันทึกเช่นเดิม

**Outcome**
- พื้นที่ที่เพิ่งบันทึกปรากฏในรายการทันทีโดยไม่ต้องรอขั้นตอน generate และไม่มีประเทศหายจากรายการเพราะยังไม่ถูก generate
- ผังพื้นที่คงสถานะที่ผู้ใช้กางไว้ตลอดการบันทึก และเลื่อนไปเน้นแถวที่เพิ่งบันทึกให้อัตโนมัติ
- ฟอร์มพื้นที่อ่านง่ายขึ้นและใช้พื้นที่หน้าจอคุ้มขึ้น ผู้ใช้ย้อนค่าที่แก้ผิดกลับได้ และได้รับคำเตือนก่อนการเปลี่ยนแปลงที่กระทบพื้นที่ระดับล่าง

---

### Organization Hierarchy In-Place Refresh

**Spending Hour:** 1.25 hr.

**Activity**
- แก้การจัดการ cache ของ organizationApi ให้ mutation invalidate tag และ query provide tag อย่างถูกต้อง พร้อมลงทะเบียน tag "Organization" ใน baseApi เนื่องจาก tag ที่ไม่ได้ลงทะเบียนจะไม่มีผลใด ๆ โดยไม่มีการแจ้งเตือน
- ตัดการ reload หน้าจอหลังบันทึกออกทั้งหมด และย้ายงานที่ reload เคยทำโดยปริยาย เช่น การปิดฟอร์มและล้างค่าฟอร์ม มาทำอย่างชัดเจนแทน
- เพิ่มกลไกกลางใน HierarchyView สำหรับเน้นแถวที่เพิ่งบันทึก โดยกางลำดับชั้นแม่ เลื่อนเข้าหน้าจอ และไฮไลต์ชั่วคราว ออกแบบให้รับ input เดียวเพื่อให้ทุกหน้าจอที่ใช้คอมโพเนนต์ร่วมกันมีพฤติกรรมตรงกัน
- รองรับกรณีสร้างใหม่ที่ยังไม่มี id จาก server ด้วยการอ้างอิงจากชื่อภาษาอังกฤษ แล้วจับคู่เมื่อรายการที่รีเฟรชแล้วกลับมา

**Scope / Context**
- อยู่ในโมดูล core ส่วน User Management หน้าจัดการโครงสร้างองค์กร (department / command / station) ปัญหาที่แก้คือทุกการบันทึกจะสั่ง reload หน้าจอ ทำให้สถานะที่ผู้ใช้กางไว้และตำแหน่งที่เลื่อนอ่านอยู่หายทั้งหมด
- กลไกการเน้นแถวถูกวางไว้ที่คอมโพเนนต์กลาง เพื่อให้หน้าจัดการบริการและหน้าจัดการพื้นที่นำไปใช้ต่อได้ในงานเดียวกันนี้

**Outcome**
- การเพิ่ม แก้ไข และลบโครงสร้างองค์กรรีเฟรชรายการในที่เดิม ผู้ใช้ทำงานต่อเนื่องได้โดยไม่ต้องกางลำดับชั้นใหม่ทุกครั้ง
- ผู้ใช้เห็นชัดเจนว่าแถวใดคือรายการที่เพิ่งบันทึก ลดการต้องไล่หาเองหลังบันทึก

---

### Service Hierarchy In-Place Refresh

**Spending Hour:** 0.75 hr.

**Activity**
- เพิ่ม invalidatesTags ให้ mutation ของประเภทบริการและประเภทย่อย ให้ตรงกับ tag ที่ query ฝั่ง case ประกาศ provide ไว้อยู่แล้ว
- ตัดการ reload หน้าจอหลังบันทึกออก และเปลี่ยนเป็นการปิดฟอร์มกับล้างค่าฟอร์มอย่างชัดเจน
- นำกลไกเน้นแถวที่เพิ่งบันทึกจากคอมโพเนนต์กลางมาใช้กับผังบริการ โดยแยกกรณีแก้ไขที่มี id อยู่แล้ว ออกจากกรณีสร้างใหม่ที่ต้องจับคู่จากรหัสหรือชื่อหลังรายการรีเฟรช

**Scope / Context**
- อยู่ในโมดูล CMS ส่วน System Configuration หน้าจัดการประเภทบริการ เป็นปัญหาเดียวกับหน้าจัดการโครงสร้างองค์กร จึงแก้ด้วยแนวทางเดียวกันเพื่อไม่ให้สองหน้าจอมีพฤติกรรมต่างกัน

**Outcome**
- การบันทึกประเภทบริการและประเภทย่อยไม่ทำให้ผังยุบกลับอีกต่อไป และแถวที่เพิ่งบันทึกถูกเน้นให้เห็นทันที
- พฤติกรรมหลังบันทึกของหน้าจัดการบริการ โครงสร้างองค์กร และพื้นที่ ตรงกันทั้งสามหน้าจอ

**Daily Estimated Effort:** 8 hr. / 8 hr.
